# Architecture Decision Records

Use an ADR only for a decision that is hard to reverse, surprising without context, and selected from real alternatives.

ADRs use sequential names such as 0001-modular-monorepo.md. Keep each record concise and link the governing OpenSpec Change when one exists.

