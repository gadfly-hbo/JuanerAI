# Core Domain Notes

Owns Product Core, Application use cases, Ports, and executable shared contracts.

Current state: no business rules or interfaces are frozen. The first vertical business scenario must reveal the minimum useful core. Infrastructure types may not leak into this domain.

