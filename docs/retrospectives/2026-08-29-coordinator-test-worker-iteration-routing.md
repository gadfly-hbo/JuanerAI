# Change Retrospective

## Identity and Outcome

- Retrospective ID: `RETRO-COORDINATOR-TEST-WORKER-ITERATION-001`
- Scope: Spec/Controller governance and Coordinator Test/Worker iteration routing
- Change class: governance-runtime boundary review, R2 evidence
- accepted baseline: `64536195f69364f731375ecc19980a9f5e62c004`
- archive path: not applicable; this retrospective does not archive or reopen a Change
- final Validator verdict: not applicable; no implementation or validation is authorized by this retrospective
- Controller acceptance: global authority audit accepted by explicit user decision on 2026-08-29
- historical verdicts preserved: `change-coordinator-immutable-candidate-validation`, `change-coordinator-candidate-before-validation`, `change-coordinator-candidate-authority-bound-validation`, and `change-coordinator-candidate-gated-validation` remain terminated and provide no dispatch authority

## Intended Value

- user outcome: preserve Test Asset Readiness, role isolation, frozen paths, and executable evidence while preventing an operating target from being treated as a hard result budget.
- intended delta: identify whether the failure came from global rules, role prompts, Change-specific clauses, historical authority drift, or current automation; freeze the minimum correction boundary before a new Change.
- reused baselines: `AGENTS.md`, `docs/governance/change-complexity-control.md`, `docs/governance/product-change-execution-policy.md`, `.ai-coding/policies/testing.md`, `.ai-coding/state-machine.md`, the four role configurations, the approved `dual-device-transition-foundation` baseline, and the existing signed `REVISION` command shape.
- non-goals: changing global governance, role prompts, templates, canonical baseline Spec, public Coordinator methods, State/Event schemas, gateways, dependencies, retries, recovery, persistent authority, or any Candidate behavior.

## Evidence Summary

| Evidence | Expected | Final Result | Source |
|---|---|---|---|
| global authority model | operating target and root-cause return are unambiguous | PASS; no global rule change required | `AGENTS.md`; `docs/governance/change-complexity-control.md` |
| Spec evidence contract | pre-implementation observer health is distinct from semantic RED and post-implementation proof | PASS in final CGV Spec; earlier CBV/CAVB contracts remain historical counterexamples | terminated Change Test Plans and CGV delta Spec |
| Test/Worker iteration policy | invalid Test returns to Test Design; frozen-contract production defect receives bounded Worker revision | PASS in global policy and resolved correction brief | `.ai-coding/policies/testing.md`; `CGV-TEST-WORKER-ITERATION-CORRECTION-001` |
| historical authority boundary | terminated OpenSpec, events, briefs, and board fields do not authorize a new Change | PASS as current user decision; prior drift remains historical evidence | `AGENTS.md`; project-control read model |
| Coordinator conformance | existing pre-Candidate signed REVISION path matches the approved baseline | FAIL; production recognizes the request shape but admits only Validator-specific/review-return states | `openspec/specs/dual-device-transition-foundation/spec.md`; `tools/harness/change-coordinator/coordinator.mjs` |
| Git/retention continuity | governance work starts without changing retained dirty bytes | PASS at branch transition | branch-switch Controller readback on 2026-08-29 |

## Effort and Rework

- Spec clarifications after Gate: multiple Candidate-series clarifications; early packages conflated downstream observer reachability with the current semantic RED frontier.
- Test corrections for the same behavior: multiple historical corrections; the second-correction stop line was sometimes handled correctly and later replaced by Change-specific terminal budgets.
- Worker revisions or replans: no Worker or Validator was released in the final CBV/CAVB/CGV attempts; their termination remains historical.
- model/reasoning upgrades: Change-scoped high support-role substitutions were used for the terminated Candidate Changes; none survives as dispatch authority for the new correction.
- broad existing-test migrations: none authorized by this retrospective.
- Validator FAIL rounds: not applicable to this retrospective.
- environment or tooling incidents: one project-board event-type attempt was rejected before write because the requested type was outside the closed schema; the existing `status_updated` event was used instead, with no schema expansion.

## Good Friction

- The authority-order audit prevented terminated OpenSpec and project-board history from being promoted over current user and Constitution authority.
- Full global-rule, role-prompt, template, automation, and historical-source review prevented a local Candidate stop line from being misdiagnosed as a system-wide defect.
- The final CGV evidence contract correctly separated helper/environment discrimination, current semantic causal RED, and post-implementation four-public-method evidence.
- The dirty-worktree branch transition required live remote identity, NUL-safe inventory, per-path byte identity, empty staged set, and nine inherited hashes before any governance write.
- Test/Worker/Validator and production remained locked while the retrospective was produced.

## Avoidable Friction

| Friction | Root Cause | Earliest Preventive Gate | Evidence |
|---|---|---|---|
| `one Test return` and `one Worker implementation` became hard result budgets | Change-specific clauses overrode the global operating-target meaning | Controller Proposal/Spec review | CBV/CAVB/CGV proposal and Test Plan stop lines |
| a Test fixture defect terminated CGV after a valid semantic RED | Controller followed the local terminal clause instead of returning the incomplete Test to Test Design | Test Design review and Controller RED review | CGV verification and superseded readiness brief |
| downstream post-Candidate observers were required through the current positive public lifecycle before that lifecycle existed | early Spec evidence contracts conflated discrimination preflight with post-implementation reachability | Spec Gate Test Feasibility review | CBV/CAVB Test Plans; final CGV correction |
| historical briefs and board labels influenced successor constraints | lower-authority historical read models were treated as reusable execution rules | successor Change intake authority audit | resolved/superseded briefs and terminated verification files |
| pre-Candidate Test/Worker correction cannot be admitted by the current Coordinator | implementation maps all non-Validator FAIL results to non-baseline `AGENT_RESULT_FAIL` and admits signed REVISION only for Validator/review states | Foundation conformance Test and implementation review | baseline AC-DTF-003-01/05/06; `coordinator.mjs` |

## Complexity Stop-Line Audit

- stop line crossed: yes
- trigger: repeated Spec/Test corrections, contradictory evidence contracts, repeated Change replacement, and a user correction that must become persistent behavior
- root-cause class: `A=no`; `B=yes` Controller execution deviation; `C=yes` primary Change-specific override; `D=yes` historical-authority promotion; `E=yes` narrow Coordinator implementation mismatch
- return Gate: retrospective first, then a new independently authorized OpenSpec Change; Test and production remain frozen until their normal Gates
- re-slicing decision: one non-Candidate governance-runtime correction, `change-coordinator-pre-candidate-revision-routing`
- quality evidence preserved: all four terminated verdicts, rejected Test assets, decision briefs, project-board events, retention bytes, and the approved baseline remain unchanged

## Reusable Outputs

| Asset | Authority/Path | Future Trigger | Reuse Rule |
|---|---|---|---|
| operating target | `docs/governance/change-complexity-control.md` | any Test correction or Worker revision | one return/implementation is a target; second same-kind correction triggers Controller root-cause review, not automatic termination |
| root-cause routing | `docs/governance/change-complexity-control.md` | any `BLOCKED`, `TEST_CONFLICT`, Test FAIL, or Worker FAIL | invalid Test -> Test Design; frozen-contract production defect -> bounded Worker revision; ambiguous contract -> Spec/Design |
| Test feasibility matrix | next Change OpenSpec only | a lifecycle change whose positive observers do not yet exist | distinguish helper/environment/observer health, current semantic causal RED, and post-implementation public evidence; do not create a public Test seam |
| signed correction command | approved `dual-device-transition-foundation` baseline | same-scope pre-Candidate Test/implementation defect | use existing signed REVISION with `revision_of_candidate_sha:null`; preserve all original DISPATCH identities and evidence binding |
| historical evidence | four terminated Change directories and project-control history | audit or root-cause review only | read-only input; never dispatch authority or a new Change default |
| retention evidence | 2026-08-29 branch-transition readback | any checkout/branch ownership change while dirty bytes remain | compare raw porcelain bytes and a separate per-path type/mode/content identity manifest; neither replaces the other |

## Lessons

| Lesson | Classification | Target | Approval Needed |
|---|---|---|---|
| Do not translate an operating target into a first-output result budget. | workflow | Controller Change intake and Gate decisions | already approved as current user authority; no global-file edit |
| A healthy semantic RED may occur before post-implementation observers are reachable. | reference | Change-specific Test Feasibility Matrix | include only when the next Spec requires it; no global template change |
| Role failure labels are inputs to Controller root-cause classification, not automatic Change termination verdicts. | workflow | Controller review practice | already approved; automation conformance requires the follow-up Change |
| Terminated OpenSpec and board history remain evidence, not successor authority. | workflow | successor intake authority matrix | already approved; do not rewrite historical files |
| Existing approved correction commands must be executable for every named baseline disposition. | follow-up Change | `change-coordinator-pre-candidate-revision-routing` | approved to start only inside the frozen Change/Git/role boundaries |

## Process and Tooling Debt

| Debt | Impact | Owner | Separate Change Required | Release Condition |
|---|---|---|---|---|
| non-Validator Agent FAIL collapses to `AGENT_RESULT_FAIL` | loses the approved phase-specific blocked reason and blocks bounded continuation | Coordinator Worker after TDD_READY | yes | focused public-method RED, minimal `coordinator.mjs` GREEN, regression, retirement Gate, independent Validator |
| pre-Candidate signed REVISION accepts only Validator-specific blocked state | same-scope Test asset or Worker correction cannot re-enter `TEST_RED` | Coordinator Worker after TDD_READY | yes | exact identity/evidence binding tests and frozen-contract implementation |
| existing Test proves nullable-Candidate REVISION with `VALIDATOR_SECOND_FAIL` | does not discriminate the missing Test/Worker routes | Test role after Spec Gate | yes, Test-only phase of the same follow-up Change | Test Asset Readiness and causal RED before production release |

## Next-Change Baseline

- behavior that must be reused: the four public Coordinator methods; closed RESULT `PASS|FAIL`; existing `SPEC_FAILURE`, `TEST_CAUSAL_RED_UNAVAILABLE`, and `WORKER_GREEN_FAILURE`; existing signed REVISION command and `revision_of_candidate_sha:null`; full DISPATCH/repository/worktree/branch/baseline/scope/CAS/evidence binding.
- decisions that must not be reopened implicitly: Spec FAIL remains `MANUAL_CONTROLLER_STOP` and cannot auto-REVISION; Test FAIL stops for Controller classification before a same-scope Test-asset REVISION; Worker FAIL exposes `next_action=REVISION`; second same-kind correction remains external Controller root-cause review with no new State counter.
- expected ordinary Change shape: one small OpenSpec package, one Change-specific Test Feasibility Matrix, focused public-method RED in `coordinator.test.mjs`, one-file production ceiling `coordinator.mjs`, focused/regression/canonical evidence, Test Asset Retirement Gate, and fresh read-only validation.
- remaining risks: a Test fixture request for `fixtures.mjs` must stop before first write and prove it cannot be expressed with the existing harness; the mixed untracked `production.test.mjs` remains physically unresolved and forbidden.
- explicitly deferred work: every Candidate behavior, validation execution ordering, `production.mjs`, `adapters.mjs`, `host-loop.mjs`, public methods, State/Event/gateway changes, dependency, retry, recovery, network, persistent authority, integration, publication, PR, merge, and archive.

## Frozen Follow-up Behavior

- Spec Agent FAIL maps to existing `SPEC_FAILURE` with `next_action=MANUAL_CONTROLLER_STOP`; REVISION does not modify a frozen Spec.
- Test Agent FAIL maps to existing `TEST_CAUSAL_RED_UNAVAILABLE` with `next_action=MANUAL_CONTROLLER_STOP`; production remains frozen. Only after Controller reads the Test artifact and classifies an in-contract Test-asset defect may a same-scope signed REVISION with `revision_of_candidate_sha:null` return to `TEST_RED`.
- Worker Agent FAIL maps to existing `WORKER_GREEN_FAILURE` with `next_action=REVISION`; a same-scope signed REVISION with `revision_of_candidate_sha:null` returns to `TEST_RED`.
- `AGENT_RESULT_FAIL` is removed from production semantics without adding a replacement BlockedReason, State field, Event, counter, or public method.
- second same-kind Test correction or Worker revision is assessed from external Controller evidence and triggers root-cause review; Coordinator does not count or auto-terminate it.
- every REVISION preserves exact original DISPATCH Change, repository, worktree, branch, baseline, scope, role/identity, expected State version/hash, and `changes_requested` evidence binding.

## Git and Retention Baseline

- branch: `work/macbook/change-coordinator-pre-candidate-revision-routing`
- branch point, local Head, remote-tracking `origin/main`, and live remote `refs/heads/main`: `64536195f69364f731375ecc19980a9f5e62c004`
- live remote target branch before local creation: absent
- raw `git status --porcelain=v1 -z --untracked-files=all` before and immediately after switch: 290 NUL records; SHA-256 `a31f8954badb9b21031815ccc048f33eb2a0eefc46b3c0399a98b9a589559f6d`
- independent per-path record/type/mode/content-or-MISSING manifest before and after switch: 290 entries; SHA-256 `0a9afc4631578783a8e9e00c904b3c22c8d112a3b9b342dddbd208569ed19569`
- staged paths before and after switch: zero
- all nine inherited production/Test hashes: unchanged from the frozen CGV retention readback
- second existing worktree and every older branch reference: preserved
- integration authority: not granted; stage, commit, push, PR, merge, branch deletion, and final integration require later explicit user approval

## Completion Criterion

- [x] Global authority, current user correction, historical Change clauses, role prompts, templates, automation, and project-board authority were cross-checked.
- [x] Root-cause classes A-E and the owning return Gates are explicit.
- [x] Four terminated verdicts and rejected Test assets remain historical and unchanged.
- [x] Reusable policy points refer to existing sources rather than duplicating them into a new global mechanism.
- [x] Tooling/code debt is isolated to a separately gated follow-up Change and is not represented as fixed by this document.
- [x] Git, retention, role, path, and integration stop lines are explicit.
