# M1/B0/S05 — authorized bounded repair cycle and Test012 brief

## Current authority and Gates

2026-09-05 latest user “授权” approves the immediately proposed S05 cycle: one temporary terra/high Test, one temporary terra/high Worker, one fresh temporary sol/high read-only Validator, serially after their Gates. Endpoint is original WVEB independent verdict and receipt, NOT Git integration/Acceptance/archive/M2/Desktop. No historical role approval reused. Configured medium roles stay unchanged; temporary exceptions end when each role returns. Boundary/R2 because containment/effect and shared evidence contracts. Missing contract/scope/evidence, a new substantive finding, or a repair outside the two helpers stops for user decision.

Controller readback20/20 R012 disposition inputs matched. Spec semantics remain the approved "/" lexical root plus at/below component containment; REQ-WVEB-001 / AC001/003/004/005 unchanged. Existing Spec Gate001 retained. Controller classified Validator003 F1 as incomplete Test coverage + frozen-contract production defect; no semantic Spec revision or broad root matrix. This supplement freezes the executable evidence allocation before Test dispatch.

Work only /private/tmp/JuanerAI-coordinator-worktree-validation-execution-boundary. Read AGENTS, applicable model-routing/testing/Retirement, existing Test012 supplement in test-plan, Validator003 Gate and current source/Test as needed. All older instances stopped.

## Frozen Test012 manifest: append five, preserve382

Sole Test writer path: tools/harness/change-coordinator/worktree-validation-execution-boundary.test.mjs.
Current entire135737-byte/1644-line prefix SHA256 0d208b95d67305c53935358975f1505a58c7383112c2f454b95009fcbf9fbabb must stay byte-identical, including original382 bodies/helpers/order; no import edits, assertion changes, deletion/renumber, new Test path, production-AST expansion or private seam.
Append N217..N221 in that exact order; expected387 unique leaves (382retained+5added). Names clearly state public behavior and IDs. Use existing addedTest and public evaluator/factory, oracleSnapshot, actualObservation/expectedReceipt and temp-Git helpers. Any support is appended, minimal, only consumed by these leaves.

| ID | Public seam and purpose | Exact behavior / expected pre-fix |
|---|---|---|
| N217 | L1 "/" nonempty descendant parent, AC001/005 | valid ordinary-root nonempty control full oracle; then worktree subject/observation root="/" with matching "/scopes" parent and raw scopes/a.txt inventory; full independent OK oracle required, actual SUBJECT_MISMATCH is expected causalRED |
| N218 | L1 "/" nonempty root-level leaf, AC001/005 | status path z-last.txt (already allowed exact rule), parent="/"; matching inventory/stat/hash; complete independent OK oracle. Equality boundary PASS pre-fix |
| N219 | L1 ordinary-root same-name-prefix sibling, AC001/005 | prove valid ordinary-root control; parent root+"-sibling/scopes" must be SUBJECT_MISMATCH. Only parent mutation, preserve siblings. Expected PASS pre-fix |
| N220 | L2 ordinary-root descendant cwd, AC003/004 | create authorized synthetic temp-Git worktree; mkdir nested cwd under it, realpath; run public gateway real child with complete24-field receipt matching execution cwd and independent snapshot/hash. Expected PASS pre-fix; no root-repository claim |
| N221 | L2 ordinary-root same-name-prefix sibling cwd, AC003/004/005 | create real sibling directory under suite-owned temporary root, lexical prefix root+"-sibling"; valid control passes first; then only cwd points to sibling, public INPUT_INVALID, no receipt/child sentinel, worktree status/HEAD/index unchanged. Cleanup under existing temp owner. Expected PASS pre-fix |

Prove fixture consistency before causal assertions: nonempty inventory correlation, real relative path/parent relation, complete independent hashes, no wrong early frontier. Do not claim every new leaf RED: expected total386PASS/1causalRED. Other failures return Test construction or substantive blocker as appropriate; never change production. Temporary probe logs only under OS temp and apply_patch for script edits.

## L2 evidence allocation frozen before Test

No attempt to create Git at actual "/"; no mocked fs/Git/process, no VM/private extraction/eval, no new public export or production test hook.
L1 N217 owns actual "/" descendant causal runtime proof. L2 N220/N221 and retained suites own real temporary-Git/child/receipt, ordinary-root equality/descendant/escape preservation. The "/" branch of the production private contained predicate is explicitly owned by exact-source Controller review plus fresh independent Validator inspection of its callers and boundary semantics, not claimed as L2 root runtime.
The production helper duplicates the same confirmed defect; minimum consistent two-helper repair is justified by public L1 causalRED + exact-source defect/call-site evidence. No assertion that production "/" has an independently executed L2 causalRED. This is the already user-approved public/source evidence split, not removal of existing evidence. If the Test role finds these obligations cannot be met in the frozen boundaries, return BLOCKED before broadening.

## Production freeze and eventual Worker scope

Until Controller Readiness + new Test SHA-bound TDD_READY, both production files are read-only:
- snapshot SHA f87236c901d1993a5627124ad16dd7c07f90ecb10764b09a4ba8348ec0a96520;
- production SHA 6ae12f06221694eb29063fa7e01b87e3287ccec60ddaa38b6a8033cc47ba8f05.
Later Worker may change ONLY snapshot private isContainedParent and production private contained. No caller/signature/exports, other guard, getter, array/root-admission, receipt/hash/process/timeouts, dependency/config or third file. Worker never writes Test. The Controller will publish an exact Worker brief after accepted causalRED and Test freeze; user cycle approval is conditional, not early TDD_READY.

HEAD33f04a35d13abe64f4394d54eec166b58cb44716, branch work/macbook/change-coordinator-worktree-validation-execution-boundary, index empty. Preserve all existing unrelated residue.

## Commands and return

Command-local PATH=/Users/huangbo/Dev/Env/homebrew/bin:/usr/bin:/bin; unset XANTHIL_REAL_PI_ACCEPTANCE.
Test runs only focused: /Users/huangbo/Dev/Env/homebrew/bin/node --test --test-reporter=tap tools/harness/change-coordinator/worktree-validation-execution-boundary.test.mjs.
Controller independent RED then after Worker GREEN runs focused387, related757 (same exact seven files as Worker004 related result), canonical ./tools/harness/validation/run (previous1410PASS/1expected real-Pi skip, actual results required), diff/scope/finalRetirement; then fresh independent Validator. Counts overlap.

You are not alone; Controller owns planning/board and other roles are stopped. Test owns only appended Test text. NO production/spec/docs/board/config/dependency/Git or other-checkout writes, external/provider/Desktop calls, delegation or installs. Suite-owned temp fixtures/logs are allowed. Read complete output, capture exact exit/totals/IDs/order, causal failure vs helper error, Test SHA/bytes/prefix proof and unchanged source hashes. Ledger all5 permanent REQ/AC regressions plus each appended helper's consumers; no retirement.

Return evidence and stop; Controller accepts Readiness. Future role write permissions begin only in separate frozen brief. No automatic repair of newly found substantive issues.

## Controller pre-Test review

All5 cases map to current public behavior, distinct equality/descendant/sibling boundaries, not generic path testing. Existing382 remains intact. Only one missing public causal case is expected RED; ordinary-root real L2 controls guard the corresponding production helper correction. Evidence limits explicit. Test-design scope PASS; Spec Gate retained, TDD_READY not yet passed.
Ponytail scope/asset-plan review: Lean already. Ship. (simplification disposition only, not Test or product completion).
