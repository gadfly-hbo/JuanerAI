# M1/B0/S04 — bounded discriminator Test supplement 011

## Current authority and dispatch stop

Completed checkpoint: the currently approved temporary instance `/root/wveb_test_correction_011` actually executed and stopped. [Test011 Gate](reviews/m1-test-correction-011-gate.md) records Controller accepted382/379PASS/3causalRED, frozen output identity and pre-Worker asset review. Route exception ended. Dispatch/pending text below is historical stage context, not a command to replay this Test or grant Worker. Latest cursor NEXT_ACTION/R010.

Current dispatch release (after R009): user explicitly approved the requested one temporary terra/high Test instance. `ROUTE_EXCEPTION_AUTHORIZED / READY_TO_DISPATCH`. Prior pending wording below is the preparation checkpoint only. This approval applies solely to Test011, its exact append-only manifest and current brief; it is not reused from Test010 and does not release Worker. Controller will dispatch a fresh default-role gpt-5.6-terra/high instance and read back actual execution.

2026-09-05 user “批准” approves the immediately preceding bounded Test supplementation stage following Validator002 FAIL. This is Test Design/evidence work only. M0–M4 v1, Spec Gate001 and existing REQ/AC/Design semantics remain unchanged. No Worker, acceptance, integration or external action is authorized.

Status: `TEST_STAGE_AUTHORIZED / MANIFEST_FROZEN / ROUTE_EXCEPTION_PENDING / NOT_DISPATCHED`. Current tool and `.codex/agents/juaner_test.toml` fix the custom Test role to terra/medium and prohibit overriding it. WVEB remains an R2 boundary Change requiring terra/high. Request one temporary default-role gpt-5.6-terra/high instance carrying the existing juaner_test duties; do not downgrade, edit config, replay Test010's expired temporary authorization or let Controller write Test code. User approval of this role substitution is the remaining dispatch release condition, not a repeated request for the already-approved Test scope.

Proposed route duration: this one Test011 return; rollback: stop the temporary instance and retain configured default without config writes. Reason/evaluation target: frozen descriptor-first/callback-zero L1 boundary, independently scheduled causalRED and unchanged baseline. Workspace-write only for the sole Test path below. Fresh bounded context, no other role or delegation.

## Root cause, seam and exact manifest

Source of truth: [Validator002 disposition](reviews/m1-validator-002-gate.md), Design §2.1/§2.3, `REQ-WVEB-001 / AC-WVEB-001 / AC-WVEB-005`. L1 schema selection consumes `kind` before descriptor admission. Existing tests target non-discriminator properties. This is one known contract violation, not a new product decision or reason to rescan all caller-owned fields. The complexity stop line returns to Test Design with production frozen; release requires healthy controls and causal failure on the zero-callback assertion.

Only the existing exported `evaluateWorktreeSnapshotObservationV1` seam is tested. No private-function calls, structural-source assertion, mock evaluator or L2 expansion. Controller freezes this additive manifest under the current approval:

| ID | Exact scheduled name | Single mutation | Required outcome |
|---|---|---|---|
| N214 | L1 before.kind accessor rejects without callback | own enumerable kind data property on a valid PRESENT before stat becomes a getter returning PRESENT | exact REJECTED/INPUT_INVALID and callback count0 |
| N215 | L1 after.kind accessor rejects without callback | own enumerable kind data property on a valid PRESENT after stat becomes a getter returning PRESENT | exact REJECTED/INPUT_INVALID and callback count0 |
| N216 | L1 content.kind accessor rejects without callback | own enumerable kind data property on valid FILE content becomes a getter returning FILE | exact REJECTED/INPUT_INVALID and callback count0 |

Preserve all132605 input bytes as an exact prefix, including379 actual registrations/bodies/helpers/order/assertions. Append only these three independently scheduled leaves in N214,N215,N216 order and minimal local support. Final planned inventory382 = existing379 +3; this is not an executed result. Each leaf owns its control, descriptor mutation check and forbidden-callback proof. No new matrix, schema variants, throwing-getter family, Proxy/cross-realm/global-prototype scenarios, second Test path, dependencies or observer framework.

Use Test-owned valid fixtures and the existing independent `oracleSnapshot`; compute the control expectation before hostile input. Ensure before/after stat records are distinct before mutation: existing `entry` defaults `after = before`, so mutating an aliased object would invalidate single-position evidence. Prove the unmodified control matches the complete oracle; preserve sibling values/descriptors, keys/prototype and non-kind fields. Inspect descriptors rather than reading the getter during fixture self-check. Getter count must be0 immediately before the public call. Evaluate once, verify exact rejection, then assert count0. A failure caused by count1 is the intended causalRED; fixture/oracle/early unrelated failures are not accepted.

Reuse the confirmed temporary Validator probe as diagnostic input only; it is not a formal test asset or production dependency. No permanent source hash constants in Test.

## Ownership and frozen input

Work ONLY in `/private/tmp/JuanerAI-coordinator-worktree-validation-execution-boundary`; branch `work/macbook/change-coordinator-worktree-validation-execution-boundary`; HEAD `33f04a35d13abe64f4394d54eec166b58cb44716`; index empty.

Sole repository write path: `tools/harness/change-coordinator/worktree-validation-execution-boundary.test.mjs`, append-only. You are not alone; preserve existing dirty content and others' changes. Controller owns planning, OpenSpec evidence metadata and board. No production/spec/config/board/Git mutation, no child Agent or external action. Use apply_patch. Unique task-owned `/private/tmp` execution logs are allowed; suite-owned isolated temp Git/child fixtures are allowed as existing tests require. Never overwrite historical evidence or caches.

Verified entry identities on this continuation:

- Test132605 bytes: `4dafee2331a10a53c370d75fc217914352d089267d0b9354b1d5b7596e890f98`.
- Snapshot: `6d9e181923aaf71dfcbb30308468c251020fe7c55717ffacc58a6cd7755bfac0`.
- Production: `6ae12f06221694eb29063fa7e01b87e3287ccec60ddaa38b6a8033cc47ba8f05`.

Recheck before dispatch and after Test execution; an identity alone grants no write authority. Approved original379 attachment remains historical retained baseline, not silently rewritten into382.

## Required reads, execution and return

Read AGENTS.md, CONTEXT.md, testing policy/definition-of-done, model routing, complexity control, Test Asset Retirement; complete seven-file WVEB package with the current Controller supplement in test-plan.md; Spec Gate001, Validator002 disposition, this brief, MASTER_PLAN/NEXT_ACTION/latest receipt. Read existing Test/production context, reuse the field audit and379 work rather than repeat them.

Use command-local PATH `/Users/huangbo/Dev/Env/homebrew/bin:/usr/bin:/bin`, unset `XANTHIL_REAL_PI_ACCEPTANCE`, check existing Node/Git/TypeScript5.9.3 without installs. Execute full focused command:

`/Users/huangbo/Dev/Env/homebrew/bin/node --test --test-reporter=tap tools/harness/change-coordinator/worktree-validation-execution-boundary.test.mjs`

Expected pattern on frozen production:382 scheduled, retained379 PASS, three callback-zero causalRED. Report actual full counts and each failure cause; do not force counts or weaken assertions. Preserve stdout/stderr/exit and runtime ID/name order. Controller independently reruns focused after return; existing related/canonical evidence stays historical until required post-Worker replay, not re-run speculatively at this Test-only step.

Return `TEST_RETURNED / PENDING_CONTROLLER_READINESS`, exact new Test hash/bytes/lines, baseline prefix proof,382 inventory, three controls/mutations/frontiers, full outputs and environment, both production unchanged, diff/scope check and lifecycle ledger. Added leaves are permanent regression under AC001/005; any local helper names their three consumers; temporary logs/probes remain outside repo; no retirement/deletion planned. Do not claim Controller TDD_READY. Stop for TEST_CONFLICT or ROUTING_ESCALATION_REQUIRED rather than expand scope.

After Controller Readiness and exact identity freeze, a separate Test-bound TDD_READY decision precedes minimum snapshot-only Worker work. S04 remains OPEN until full GREEN/regression/Retirement and a fresh Validator pass the original M1/B0 verification point. B0 additionally requires normal M1 acceptance/integration/archive/live-main. No M2 or Desktop action.
