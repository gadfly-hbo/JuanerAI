# Xanthil Desktop Engineering Controller Takeover 001

Date: 2026-09-24. This is a responsibility handover for the existing Change,
not a new Change, execution package, product approval, or resume instruction.
Retained Change: `xanthil-desktop-membership-repurchase-decision-case`;
product package: `PKG-XANTHIL-DESKTOP-001`.

## Read-only facts

- Integration baseline observed before this governance change:
  `main = origin/main = f59fe3dab368792834a02c8b408e6848b9ff5fbb`.
- The retained WIP branch
  `origin/work/macbook/wip-preflight-exception-disposition` was observed clean
  at `c01b68b639156344b2257e45a69892268c17693f`. It diverges from current main;
  this takeover does not merge that branch or copy its history.
- The published bounded supplement is
  `docs/planning/2026-09-20/xanthil-desktop-b2-command-context-001.md` at
  `c01b68b`. It has not been confirmed as adopted by Mini.
- Continue only the original Mini task `纵切-1.0`,
  `01a0b45a-13ad-7b92-a2b4-cdde980558e1`, repository
  `/Users/bendandebaba/JuanerAI`, branch
  `work/macbook/whitepaper-blueprint-v1`. Responsibility transfer does not
  rename, duplicate, migrate, or write that task or branch.
- The latest confirmed Mini intake is `8078b9ab3d09ee8cf9507c5718a1e366be0fa84b`,
  tree `bfee3bbd2603ebc79af789220ae5bb091eb39852`.
- The exact stop is `TEST_CONFLICT / U1.1_B2_TEST_EVIDENCE_CONTEXT_BLOCKED`:
  B2 Test-content round 1/2 is consumed, zero B2 Test files were changed, zero
  B2 tests ran, and there is no B2 RED, TDD_READY, or Worker. The retained B1
  scoped GREEN and affected Spec Gate PASS remain evidence, not
  first-package/GUI/product acceptance.
- The current supplement permits only the remaining B2 Test-content round 2/2
  for its named context-health and F1-F4 closure. Historical failures, UNKNOWN,
  unavailable evidence, deviations, source/copy identities, and all consumed
  budgets remain unchanged.

These facts come from the named Git objects, WIP project-control snapshot, and
a read-only check of the original Mini task's latest completed receipt during
publication preparation. They are not a fresh remote filesystem inspection or
a claim that Mini remains online or unchanged after that observation.

The receipt names this Mini-owned persistent artifact:
`/Users/bendandebaba/JuanerAI-artifacts/xanthil-desktop-membership-repurchase-decision-case/mac-mini-wip-preflight-20260919-001/technical-decision-001/b2-test-contract-disposition-receipt-001.md`
(6,826 bytes, SHA-256
`70f864abebeee84df62e2e8735a6b77b7c4817fdf14f4c8b29b0f4f2d5d69898`).
Mini must read back its actual bytes and all required evidence locally; this
publication has not independently read that remote artifact.

## Retained WIP constraints

Only necessary rule deltas are carried from `c01b68b`; its task packages and
historical evidence remain at their original Git identities. In particular:

- `agent-model-routing.md` and the four role configurations retain approved
  ordinary `high` / R2 `xhigh`, frozen UI input, and actual-session capability
  checks; no product role is dispatched by publication.
- The sole execution policy retains bounded correction, low-risk static-read
  classification, separate execution/rework accounting, evidence standards,
  compatible technical decisions, and no reset or replay. Its named section
  anchors remain usable by existing packages.
- The B2 supplement remains a separate stopped, unadopted input: its bounded
  command-context amendment permits the extra `JUANERAI_TEST_EVIDENCE_DIR` only
  for named emitting B2 commands with per-command/per-attempt evidence paths;
  retain original/derived capture identities, positive H-B2-LOAD health plus
  missing/mismatched-context pre-spawn controls, F1-F4 and TSX assertions, B1
  regression, and only remaining Test-content round 2/2.
- Preserve all 34 local assets from the receipt (3 stage records, 31 otherwise
  unchanged assets), the five unchanged Test files, prior Gate evidence, and
  consumed round 1/2. Do not replay successful Stage0/1, P1-P4, or B1 preparation
  merely for takeover; genuinely affected regression still applies.
- Final91, real UI verification, regression, Test Asset Retirement, independent
  Validator, Engineering Acceptance, and required user Product Acceptance remain
  required. No frozen input or historical verdict is retrospectively approved.

## Responsibility adopted after receipt

After the rule change is integrated into `origin/main` and Mini returns the
receipt below, the original Mini task becomes the Engineering Controller for
the existing Change. It owns engineering feasibility, OpenSpec/design/contracts,
stage Gates, role dispatch, budgets and exceptions, engineering state,
Engineering Acceptance, and Git delivery only within separately granted
permissions.

Only the prospective authority clauses that require MacBook technical sign-off
or reserve current engineering-state writing to MacBook are superseded. After
receipt, Mini is the sole writer of current Change stage records and the existing
project board; MacBook does not maintain a parallel board. Other frozen product,
path, operation, budget, evidence, and stop constraints remain binding. This
takeover itself writes no project state or old Host Loop records.

MacBook remains Product Manager for the frozen product objective, scope, UI,
business semantics, product acceptance criteria, non-goals, and prohibitions.
It is not a required technical reviewer, engineering-state relay, or approver of
ordinary contracts, commands, environments, tests, repairs, PRs, or merge.

Mini asks the user directly in the original task about product ambiguity, scope
or upper-boundary change, extra budget, residual risk, missing permission, or
unknown/unsafe effects. Independent Validator PASS is not Engineering Acceptance
or user Product Acceptance.

## Adoption receipt and effective condition

The new responsibility rule becomes effective for this Change only after:

1. this governance change is squash-merged into `origin/main`;
2. the user forwards the takeover prompt to the original Mini task;
3. Mini reads the exact merged rule commit and this note (for example through
   `git show <rule-sha>:<path>`, without switching, resetting, or merging into its
   dirty owned branch), then read-only checks
   its actual task, repository, branch, HEAD, worktree, status, evidence
   locators, latest receipt, consumed budgets, and `c01b68b` supplement; and
4. Mini returns one receipt containing:
   - merged rule commit/tree and this file's hash;
   - task/host/repository/branch/HEAD/worktree readback;
   - current stop point, retained evidence/UNKNOWN, and consumed budgets;
   - `ENGINEERING_CONTROLLER_ADOPTED / EXECUTION_STOP_RETAINED`;
   - confirmation that no product role, test, implementation, dependency,
     provider, real-data, WIP, Git-delivery, merge, archive, or release action
     occurred; and
   - one consolidated list of decisions or permissions it must ask the user
     directly.

Until all four conditions hold, adoption is `UNCONFIRMED`. A receipt confirms
responsibility only; it does not receive the B2 supplement, resume Test, release
Worker, change project-control, or widen Git authority.

## Decisions for Mini to ask the user directly

- whether to resume the stopped Change and receive/apply the published
  `B2 Command Context 001` supplement;
- whether to add merge/archive permission for this existing Change when it
  eventually reaches that Gate. The frozen package already permits only
  post-Validator commit, branch push, and one PR; `c01b68b` adds no new Git
  delivery, merge, archive, or release authority; and
- any later request for extra budget, residual-risk acceptance, scope or upper
  boundary change, permission, or unknown-effect disposition.

No other decision is created by this takeover.

## Known tool-label compatibility

The existing project-board status CLI and static fallback use the literal
`controller` for an actor/display label. They do not enforce a device or grant
authority. Under the new rule, a new current-mode event with that compatibility
label can only be written by the Mini Engineering Controller. Renaming the enum
and fallback UI requires a separate bounded tool Change with old-event
compatibility; this takeover does not modify or restart that tool.
