# PKG-XANTHIL-DESKTOP-001 — Directed Spec Closure 001

## Authority and endpoint

On 2026-09-19 the user replied **“授权”** to the Controller's recommendation
for one targeted formal Mac mini Spec correction of F1–F6. This supplement
records that authorization and is published through the existing Git handoff
for manual forwarding to the original task. It authorizes exactly one new
substantive Spec return, whole-package read-only review and the affected Spec
Gate, then a receipt and stop. **Even a Spec Gate PASS does not release Test
under this supplement.** It does not authorize an automatic message, new task,
new product plan, UI remake, installation, provider call or production work.

This is a Foundation/bootstrap R2 contract-closure correction within the already
approved outcome, not a new product/architecture decision. It supplements
[Incremental Contract Amendment 001](xanthil-desktop-incremental-contract-amendment-001.md)
and [Execution Reslice Amendment 001](xanthil-desktop-execution-reslice-amendment-001.md).
Only the consumed Spec-return limit, the precise F1–F6 clarifications below and
the narrower stop-after-Spec-Gate endpoint change. All final UI, 91 AC, Run 3.0,
full schema, compatibility, role isolation, safety and final acceptance duties
remain. No historical budget or failure is reset.

| Identity | Binding |
|---|---|
| Package / Change | `PKG-XANTHIL-DESKTOP-001` / `xanthil-desktop-membership-repurchase-decision-case` |
| Mini task | `纵切-1.0 / 01a0b45a-13ad-7b92-a2b4-cdde980558e1`; no new task or migration |
| Mini repo / device | `/Users/bendandebaba/JuanerAI` / `mac-mini` |
| Mini branch / upstream | `work/macbook/whitepaper-blueprint-v1` / `origin/work/macbook/whitepaper-blueprint-v1`; unchanged |
| Intake parent | `0cdc3c2dfa8e3482dd3c7e6b6e55aa6bb12c0cdf` |
| MacBook publication branch | `work/macbook/wip-preflight-exception-disposition`; MacBook never writes Mini's branch |
| Latest reported Mini stop | `BLOCKED_NEW_DIAGNOSTIC_EFFECT_UNKNOWN`; no current Spec Gate PASS |
| New allowance | Directed Spec Closure 001: 0/1 used before dispatch; at most one substantive return |
| Retained budgets | Reslice Spec 1/1 and Incremental Spec 1/1 consumed; extra Test 0/2 remains unused and suspended; older Test history unchanged |

## Evidence disposition and unchanged input

The user accepted the disclosed historical temporary-path residual risk and
authorized the preceding evidence correction/read-only review. Adopt
[Diagnostic Disposition and Read-only Review 001](xanthil-desktop-incremental-contract-diagnostic-disposition-001.md)
as the Controller disposition, not as a clean execution or Spec Gate PASS.
The recorded commands/outer exits were partially recovered from existing
history, not replayed. Pre-write identity and possible prior-content loss at
the two temporary paths remain UNKNOWN. Preserve the original exception,
disclosure and missing facts; do not inspect, recreate, delete or replay those
paths/commands to manufacture evidence. The excluded Controller-review grep
diagnostic is not evidence for F1–F6 and grants no new risk waiver.

The unique Mini evidence root remains
`/Users/bendandebaba/JuanerAI-artifacts/xanthil-desktop-membership-repurchase-decision-case/mac-mini-wip-preflight-20260919-001`.
Under its `technical-decision-001/`, intake binds:

- `incremental-contract-stop-index-001.json`: 229492 bytes, SHA-256
  `5337aa200027236779746971a37384f2185104124fdb9361861170165c4f9ca2`;
- `incremental-contract-exception-receipt-001.md`: 11023 bytes, SHA-256
  `4572dc089ba842829990f6f5622d4acabe24a71148e96a1c63213ea733128fb9`.

The disposition report enumerates all twelve draft identities. The stop index
binds all 31 current files and their stopped copies, including the nineteen
non-Spec assets that must remain byte-identical throughout this Spec-only turn.
MacBook's raw correction/review records are at the device-local locator in that
report; the Git report conveys the reviewed conclusions, not a complete raw
evidence backup. Do not require Mini to rediscover or rerun the accepted review.

## Exact correction scope and exit checks

Use the report's F1–F6 source citations and these minimum checks. Spec decides
the exact closed internal mappings within the approved contract; it must not
invent public fields or weaken final assertions to make the documents agree.

| ID | Required correction and static exit check |
|---|---|
| F1 | Replace the universal command UUID prerequisite with the existing per-method minimal envelope. The six requests already lacking command ID keep that shape; only requests already defining it validate it. Check both active U1 calls and valid-envelope inactive rejection, retaining sender/version checks and full validation for active routes. |
| F2 | Make the real Store.listSessions read edges explicit: build ProjectOpenValue.sessions and resolve the authoritative current case/revision for openSession/readProjection. Name existing not-found/stale/error translations and receipt/owner validation. No new Port, fabricated owner or Renderer/Main cache authority. |
| F3 | Add one compact M1.1–M1.5 prefix table identifying active public methods and union tags, separately exact Application/Store calls and their dependencies. Every other route retains the existing safe refusal; completed I1 still implements all seven approved U1 operations. Align the table with source/build tuples without a new selector/framework. |
| F4 | Before R-U1.5, R-U3.3 and R-U4.3 E2E, package and freeze the latest already-GREEN predecessor U1.4, U3.2 and U4.2 respectively. These are later Test/Worker health inputs, not commands to run during Spec and not RED. Preserve post-Worker new-package E2E GREEN; a stale package may not stand in for the causal target failure. |
| F5 | Name the final CF-restoration RED/GREEN contract checks in the existing approved XCLI-021/runner Test paths, distinct from passing environment-health checks. Put them in the applicable R/G matrix and exact role write set. Prove retirement of C1a–C3 acceptance while preserving exact final P4/P5 and full-runner obligations; no new harness or test allowance. |
| F6 | Remove contradictory executable instructions: reference the existing frozen command IDs and their exact expanded argv/cwd/env rather than bare node/npm/runner alternatives. All active command lists must agree. No toolchain, dependency, source or script-permission change. |

Keep all twelve drafts mutually consistent, but only change text required by
F1–F6, traceability and truthful current status. Replace stale current readiness
claims; preserve their historical frozen versions and failure records. Do not
re-author the whole package, change final 79 Desktop + 12 compatibility ACs,
alter Run/schema/UI semantics, add a generic framework, or prematurely implement
the future methods whose precise refusal is already approved.

## Role, paths, commands and evidence

After verified intake, the Mini coordinator dispatches one fresh formal
`juaner_spec`, configured `gpt-5.6-sol`, requested `R2 / xhigh`, with the complete
twelve drafts, this supplement, the disposition and retained authority inputs.
Verify the named role/effort is available, record requested versus actually
observable settings, and never substitute a generic agent or downgrade silently.
The route applies only to this return; it grants no sandbox or scope expansion.

Allowed role edits are only these existing files below
`openspec/changes/xanthil-desktop-membership-repurchase-decision-case/`:

`decision-brief.md`, `dependency-decision.md`, `design.md`, `path-contract.md`,
`proposal.md`, `specs/local-analysis/spec.md`,
`specs/xanthil-desktop-decision-case/spec.md`, `structure-decision.md`,
`tasks.md`, `test-plan.md`, `traceability.md`, `verification.md`.

No need to edit every file. Mini coordinator writes only the existing
verification/traceability/tasks stage records and appends necessary raw evidence
under the original persistent evidence root; it does not substitute for Spec.
Spec/Reviewer may statically read the frozen inputs and bounded relevant
approved source. No product-module import, test, build, package, install or
provider command is authorized in this Spec-only stage. Ordinary static reads,
Git diff/whitespace checks and file hashes need no per-command approval; use
literal, safely quoted paths/patterns with no shell interpolation of Markdown.
Do not make temporary lists or run dynamic modules just to inspect text.
Capture actual argv/cwd, output and numeric exit for diagnostic commands; do not
discard stderr or infer success from a completion label. Preserve failed output.
Known-safe in-scope execution corrections retain existing cumulative limits;
this is not another correction-budget grant. New unknown effects or exhaustion
stop with one consolidated receipt, not a fresh repair loop.

The one substantive Spec return consumes this 1/1 allowance even if blocked.
Review its whole twelve-file package and complete diff, including ponytail-review
and all F1–F6 exit checks. The delegated Mini coordinator may record the affected
Spec Gate PASS only on real closure. A complexity-only PASS is not that Gate.
If any material gap remains, return its exact cause/minimum correction without
a second Spec return. Neither Test quota nor Controller edits can finish Spec.

After PASS or failure, freeze the current twelve files and full diff under the
existing evidence root, independently read back bytes/hashes, confirm all
nineteen non-Spec files unchanged, and return one receipt stating each F1–F6
result, Gate verdict, consumed 1/1 allowance, remaining Test 0/2 and next stop.
**Stop before Test dispatch or executable validation, Worker and Validator.**

## Git intake and prohibitions

MacBook publishes only the Controller disposition, supplement, planning/status
records and associated review. A separate publication receipt supplies the exact
commit/tree/unique parent, complete changed-file list and hashes. This document
does not prove its own publication, delivery, intake or successful Spec closure.

Mini verifies the current index and all 31 sources/copies, records the pre-intake
identity, fetches the exact publication ref, checks fixed commit/tree/parent/full
diff/hashes and non-overlap, then performs one ff-only intake on its existing
branch without changing upstream. Prove all 31 bytes unchanged after intake.
If the same commit is already received, verify and skip the merge; if the new
allowance has already been consumed, return its recorded result without dispatch.
Drift, unexpected paths or conflict stops: no reset/stash/clean/overwrite, early
commit, branch migration or checkout of the MacBook branch.

Keep Stage 0/1, WIP_DISPOSITION_COMPLETE, approved tool health and P1–P4 evidence.
Do not rerun successful probes, installation, WIP or sudo; do not touch pointer,
State, pause, Ledger, old worktrees, Host Loop, services, real data or providers.
No Mini commit/push/PR/merge/archive/release or project-board write is authorized
at this Spec-only endpoint. Old CHG-xanthil-desktop-session-bootstrap remains
BLOCKED / DISPATCH_ORPHAN_READY, not CLOSED.

Preparation review: [Independent Readiness Review 001 PASS](reviews/directed-spec-closure-readiness-review-001.md).
Its verdict concerns this bounded handoff only, not the corrected OpenSpec or
Spec Gate. Fixed publication and verified Mini intake remain required.
