# Dependency/Structure Development-Readiness Review 001

- Date: 2026-09-19
- Package: `XDESK-DECISION-001` v1.0, draft pending overall user approval
- Reviewer: fresh read-only support Agent `/root/desktop_decision_readiness_001`
- Route: default support Agent, inherited parent model, explicitly requested `xhigh`;
  R2 scope: persistence, transaction, Runtime, security and dependency boundaries.
  The tool does not independently echo actual runtime model/effort.
- Role boundary: not formal Spec/Test/Worker/Validator; no writes or production authority.
- Reviewed input: 35,732 bytes, SHA-256
  `63afb37c009778ab3ba6043e3462075dc895579fa58e4fc4bce0cd8ae27ab3a2`.
- Preserved input, MacBook-owned:
  `/Users/huangbo/JuanerAI-artifacts/PKG-XANTHIL-DESKTOP-001/dependency-structure-decision-20260919/review-001-input.md`.
  Byte count, shasum and OpenSSL readback agree. This device-local copy is not a
  cross-device backup; the canonical correction remains in the planning document.
- Context: decision package plus its explicitly linked current formal attachments
  and authority documents; no research repository, remote drafts or conversation
  rationale supplied to rescue missing content.
- Verdict: **NEEDS_CLARIFICATION**. This historical verdict is not rewritten.

## What I Would Build

After overall user approval, Mini performs isolated P1–P3 lock, scripts-off
dependency and synthetic packaged health probes, then closes the existing
OpenSpec. Spec Gate precedes canonical manifest/lock adoption and the isolated
Test → RED → Worker → Validator sequence. The professional Session/Case,
SQLite-versus-artifact split, separate Run/Assistance identities and Draft-only
model effects are clear.

## Required Guessing

One material gap: P3 permitted an official Electron download and checksum file
but did not specify the already-bound trust anchor or require expected/actual
binary digest comparison. A same-origin downloaded sidecar alone cannot supply
that independent anchor. No other load-bearing product/state/permission gap
was identified in this review.

## External Study Required

None. Actual dependency, Electron SQLite and Playwright compatibility are
correctly deferred to the explicitly authorized synthetic probes, not invented
as current PASS evidence.

## Untestable Requirements

P3 could not yet prove the binary matched an approved supply-chain identity.
The other success, rejection, interruption, duplicate, recovery, cancellation
and zero-side-effect endpoints were testable at planning level.

## Correctly Deferred

Exact TS/SQL names, tables/indexes, physical staging details, build config paths,
actual lock bytes and executable compatibility belong to Spec/Design or the
bounded probes. Existing UI revision/terminal/reopen rules and private Pi session
identity remain intact.

## Required Plan Additions

Before any script execution, bind the precise Electron installation entry,
locked package integrity, permitted platform/source and expected binary digest
or independently verifiable official signature/manifest anchor. Record the
observed digest and prove a match; stop on entry/source/platform/anchor/script
drift. Do not rely solely on a newly downloaded same-origin checksum sidecar.

## Controller disposition

Accepted this finding. Section 11 now selects the digest shipped inside the
lock-integrity-verified Electron npm package as the anchor, freezes the reviewed
entry and expected artifact before execution, requires comparison before use,
and rejects mirror/checksum overrides. Missing support returns to Controller.
No installation or probe was run. Section 7 also makes the already-required
pre-publication deadline/cancellation recheck explicit. A fresh support review
must inspect the revised package; this review does not imply that it passed.
