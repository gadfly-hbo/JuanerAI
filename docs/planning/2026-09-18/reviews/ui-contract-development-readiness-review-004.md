# Xanthil Desktop UI Contract Development-Readiness Review 004

## Review control

| Field | Value |
|---|---|
| Reviewed input | Corrected v1.1 package after Review 003 |
| Reviewer | New independent read-only support Agent with implementation-worker perspective |
| Route | `gpt-6-astra / high` |
| Isolation | No prior reviews, research repository, Demo source, chat history or unstated rationale supplied |
| Review scope | Readiness to revise the simulated clickable UI Contract only |
| Date | 2026-09-18 |
| Verdict | `NEEDS_CLARIFICATION` |

## 1. What I Would Build

The accepted PX-004 professional workbench and PX-006 dual-mode shell, with the
full visible capability inventory and the narrower membership-repurchase Case.
Quick mode and child capabilities remain explicitly simulated; the professional
path demonstrates the complete first Case and no production capability is
activated by this UI work.

## 2. Required Guessing

1. The matrix made candidate comparison create the Decision Closure directly,
   while UI Contract v1.0 requires a valid comparison first and a separate
   `完成分析案例` action to append the Closure and enter Completed. It must also
   remain valid to compare two candidates without selecting a preferred one.
2. The initially read matrix did not define which report remained current when
   a supplemental child result was adopted after Finding acceptance and then
   rejected. The Controller corrected this during review; a fresh review must
   verify the frozen bytes.

## 3. External Study Required

None. The eight self-contained reference plates and written package were
sufficient; the research repository was not needed.

## 4. Untestable Requirements

- valid two-candidate comparison with no preferred candidate, save/close before
  completion, and the separate completion action;
- the frozen final behavior for accepting then rejecting a supplemental child
  result and selecting the correct report for completion.

## 5. Correctly Deferred

Pixels, component choices, production schemas, Runtime, real models, packaging
and production Fork/Subagent activation remain correctly deferred.

## 6. Required Plan Additions

- Restore v1.0's two-step closure: prepare a valid candidate comparison or
  insufficient-evidence choice, then use a separate completion action to append
  the Decision Closure, finalize the report and enter Completed.
- Freeze and re-review the already corrected supplemental-report current/
  rejected/superseded rules.

## 7. Verdict: NEEDS_CLARIFICATION

The visual and child-conversation package is self-contained. The remaining
completion-authority mismatch must be corrected and checked by a fresh
Reviewer. No clickable UI or production work is authorized by this verdict.
