# PKG-XANTHIL-DESKTOP-001 — U1.1 First Package / GUI Admission 001

## 1. Decision and current status

On 2026-09-20 the user authorized this minimum execution-contract supplement:
retain the completed IPC GREEN, close first-package and real-GUI test admission,
publish on MacBook, and manually forward to the original Mini task.
**Independent [development-readiness review001](reviews/u11-first-package-gui-admission-readiness-001.md): PASS.**
Authorized for fixed publication/manual forwarding; Mini intake is unconfirmed.
This is an R2 foundation/bootstrap sequencing repair inside U1.1, not a new
product plan, UI, dependency, Runtime, data permission or long-term process.
It adds no permanent orchestration machinery.

Controller attribution: the previous Spec Gate checked static contract closure
but missed how the first build could lawfully become available for GUI RED.
A single missing-IPC assertion never admitted all preload/Renderer/build behavior.
The Mini coordinator correctly stopped; another map-health repair is not needed.

| Binding | Value |
| --- | --- |
| Package / Change | PKG-XANTHIL-DESKTOP-001 / xanthil-desktop-membership-repurchase-decision-case |
| Original Mini task | 纵切-1.0 / 01a0b45a-13ad-7b92-a2b4-cdde980558e1 |
| Mini repository | /Users/bendandebaba/JuanerAI |
| Mini branch / upstream | work/macbook/whitepaper-blueprint-v1 / origin/work/macbook/whitepaper-blueprint-v1 |
| Current HEAD / intake parent | 15ddbf45b6b5efb71128c61fc78b3afb13536fe6 |
| Parent tree | fc6fffecb03aaaa3e3cd6eb79eabab37aef06797 |
| MacBook publication branch | work/macbook/wip-preflight-exception-disposition |
| Stop | U1.1_INITIAL_SEAM_GREEN_CONTINUATION_GATE_BLOCKED |

No new task, branch switch or automatic message. The publication receipt supplies
the actual new commit/tree/unique parent and every changed-file hash. Only fixed,
verified ff-only intake in the original task activates the Mini instructions.

## 2. Retained evidence and exact delta

Mini's original persistent root remains:
/Users/bendandebaba/JuanerAI-artifacts/xanthil-desktop-membership-repurchase-decision-case/mac-mini-wip-preflight-20260919-001

Its technical-decision-001/u11-map-health-disposition-stop-index-001.json is
116315 bytes, SHA-256
e2cffd46bca1ffb559d05e86e6bcffc770fd347725e57d0904db103fe08d58c4.
It binds 33 current sources and 33 frozen copies. MacBook independently matched
those 66 identities, the index, and 18 result/output identities, and read raw R/G.

Retain H-SYNTAX/H-MAP/H-TUPLE/H-RUNNER PASS and the one-leaf IPC RED→GREEN:
the full twenty-method inactive Main boundary, sender-first/minimal-envelope
rejection and clone-safe zero-business-effect refusal. This does not establish
complete AC009, a healthy C1a package, GUI or U1.1 completion.

- IPC source: packages/contracts/xanthil-desktop-ipc.ts, 31470 bytes,
  eafb984e27446d98c34c96332d77aa2baa410ee211acc8e07b46fe461d40928b.
- Main source: apps/desktop/main.ts, 5826 bytes,
  dd93247b1b81741a1abfafcaf1f182ece85e8a3c52abd0fad4c6d1151ba37c64.
- R-U1.1 result: ca78e844ae456989d93059eeaf7acb616b40dea1cfc7d53564ad9f21437359af.
- G-U1.1 result: 1747c133a8483fe8d26c7df9e547d1222f6df7472c62e4fe6b8ce8e7b21259d8.

This supplement overrides only the IPC-only R/G-U1.1 selection, the claim that
one such GREEN admits the whole shell, the E-U1.1 GREEN-only restriction, and
consumed role budgets to the exact extent below. It permits the three remaining
evidence groups below inside the existing U1.1, not new product units or an
unbounded loop list. Existing assertions/history stay; no successful IPC R replay
to manufacture RED. Re-run affected GREEN as regression when its inputs change.

The approved C1a source graph and public twenty-method contract stay unchanged.
A first C1a build may have the real initial shell and still-unimplemented local
interactions; those gaps are explicit developmental incompleteness until GUI
RED→GREEN. This does not permit a second app, blank replacement UI, fake data,
alternate bootstrap entry, runtime phase selector or fabricated successful action.
The full accepted UI hierarchy and U1.1 interaction obligations must close before
U1.2. No Project/Session/analysis/persistence/provider behavior is released here.

## 3. Close the cycle once, on Mini

After intake, one fresh formal juaner_spec / configured model / R2 xhigh return
may revise only execution/verification portions of these eight existing files:

- openspec/changes/xanthil-desktop-membership-repurchase-decision-case/proposal.md
- openspec/changes/xanthil-desktop-membership-repurchase-decision-case/design.md
- openspec/changes/xanthil-desktop-membership-repurchase-decision-case/test-plan.md
- openspec/changes/xanthil-desktop-membership-repurchase-decision-case/path-contract.md
- openspec/changes/xanthil-desktop-membership-repurchase-decision-case/dependency-decision.md
- openspec/changes/xanthil-desktop-membership-repurchase-decision-case/tasks.md
- openspec/changes/xanthil-desktop-membership-repurchase-decision-case/traceability.md
- openspec/changes/xanthil-desktop-membership-repurchase-decision-case/verification.md

Read the whole twelve-file package; both Requirement files, structure-decision.md
and decision-brief.md remain byte-frozen. Final 91 AC, public types/requests,
storage schema/Run3.0, accepted UI and dependencies remain unchanged. Formal Spec
freezes the exact test titles/assertions, concrete command envelopes, source
loading and health method, Worker behavior/path subsets and next handoff for
all three groups before Test. No test/product execution by Spec.
Complete-package ponytail and affected Spec Gate must pass; the coordinator
checks the entire first-package chain, not merely whether a table was filled.

### The three remaining groups, serially

| Group | Test evidence before Worker | Permitted minimum implementation | Completion/next gate |
| --- | --- | --- | --- |
| B1 — real bridge and native entry | Through the existing public Main/preload exports and normal entry, test the closed twenty-method bridge, one-for-one channels/results, real Main refusal wiring and required native window/sender safety configuration. Each genuinely absent approved capability needs its own healthy explicit assertion; existing IPC evidence stays GREEN. | Real Main startup/wiring and preload only, including minimum existing approved build configuration necessary to load those exact sources; no Renderer/business implementation. | Focused bridge/entry GREEN; retain real-runtime security as still unproved until packaged GUI. |
| B2 — real Renderer initial surface / first package | Test the actual XanthilDesktopApp export and normal mount against the accepted initial UI structure/content, honest Preview/development-incomplete labels and no business invocation during initial rendering. A healthy exact missing Renderer capability may admit only its minimal initial implementation; assertions that now execute must be classified independently. | Same production Renderer/index/styles plus necessary existing root/build/toolchain/runner wiring for C1a. Initial shell only; no speculative later interactive or business behaviors. | Focused initial-surface GREEN, exact C1a health/typecheck and first real Forge package; successful launch/control health then admits GUI tests, never counts as GUI RED. |
| GUI — remaining U1.1 user behavior | On that healthy frozen production app, run the existing U1.1 GUI assertions for mode/stage visibility, Preview, search/drawer/modal/focus, two sizes and applicable real runtime safety. Each failed intended behavior is causal RED only after app/control health. Already-passing leaves remain baseline GREEN and release no speculative work. | Only implementation needed for those actual failed UI/safety leaves, keeping all twenty business routes inactive and all future modules absent. | Rebuild on changed source, prove same-leaf GUI GREEN plus prior B1/B2/IPC regression and U1.1 closure; then resume the original U1.2 sequence without user relay. |

Test and Worker are separate formal contexts; scope-specific TDD_READY precedes
each Worker group. A first missing export does not approve assertions behind it
or an entire module: admit only the associated minimum capability; after it
exists classify the now-reachable behavior leaves, keeping the same group and
original accounting. This is normal sequential RED→GREEN, not a new correction
when the test is healthy. No deliberate regression or fake RED for existing code.

B1/B2 may use existing approved TypeScript/React facilities in the existing
Test driver for exact TS/TSX sources and static rendering; no new dependency,
loader framework or product test entry. Test-process substitutes are confined
to external Electron transport/window and DOM-mount boundaries, not the real
Main handlers, bridge mapping or XanthilDesktopApp under test. Capture actual
boundary calls/values, verify controls independently, restore within the owned
test process. Never add a production branch/flag to select a test substitute.
Static rendering proves initial markup only, not GUI input, focus or security.

Any required compile/load output is restricted to a fresh child under the
original evidence root, with its fixed source graph and inputs recorded.
Do not generate an Electron entry, second package or fake product module.
Compiler/loader/control failures are INVALID health, not behavior RED; Test must
prove the narrow loader/control with known positive/negative inputs before use.
If the installed approved facilities cannot support this bounded loading method,
stop rather than add a toolchain, alternate environment or broad adapter.

Build configuration is an implementation prerequisite of the admitted B1/B2
behavior, not a separately invented config RED. Until B2 completes the C1a graph,
do not claim an arbitrary partial graph is a new accepted tuple or run a full
typecheck against knowingly missing entries. B1's isolated source/driver health
must still be valid. At B2 completion exact C1a typecheck/package health is
mandatory; no fake exports, empty files, casts-to-full, skips or relaxed types.

## 4. Commands, path ownership and budgets

Keep the original absolute Node/npm, repository cwd, ten-field clean child
environment, 180000ms limit and corrected existing capture. No new capture tool.
The existing test-plan command table remains authoritative for unchanged work.
Spec may add only concrete U1.1 B1/B2 contract R/G selections in the existing IPC
contract test, their necessary source/control health, and GUI RED/GREEN selections
in the existing E2E file. It must freeze complete argv and any needed Node test
flag explicitly; no blanket command, flag, shell expansion or ambient environment.
GUI RED uses the first healthy B2 package; GUI GREEN/regression uses a newly
frozen package of changed production inputs. Existing H-TYPECHECK, B-PACKAGE and
E forms are reused, with their corrected evidence meaning recorded.

Existing successful R/G inputs/results are immutable. New command IDs/selectors
must distinguish B1, B2, GUI and retained IPC. A command-level failure with some
unreached leaves is not RED for all selected behavior. Require actual selected
counts, per-leaf classification, command/input identity and full stdout/stderr,
numeric child/capture exits. Unchanged successful prep is not repeated; changed
inputs require their affected syntax/control/regression checks. GUI control or
package health failure stops before a business verdict.

Test may edit only U1.1 portions of these existing paths; freeze a narrower set
for each group before dispatch:
- tests/contract/xanthil-desktop/xanthil-desktop-ipc.contract.test.ts
- tests/e2e/xanthil-desktop/xanthil-desktop.e2e.test.ts
- tests/fixtures/xanthil-desktop/desktop-contract-drivers.ts
- tests/fixtures/xanthil-desktop/desktop-e2e-harness.ts
- tests/fixtures/xanthil-desktop/desktop-fixtures.ts
- tests/fixtures/xanthil-desktop/coverage-map.ts
- tests/unit/xanthil-desktop/coverage-map.test.ts
- tests/integration/xanthil-local-analysis/local-analysis.integration.test.ts
  (existing TEST-XCLI-021 configuration oracle only, no CF/other business change)
- tools/harness/validation/run.test.mjs
  (existing incremental runner-control expectation only, no CF/old weakening)

The repaired manual-marker guard, 91 final obligations, CSVs, future behavior
tests and historic assertions are retained. Update mapping only to name actual
U1.1 evidence leaves; titles alone never prove coverage. Any prior test defect
is disclosed and charged as correction, not hidden inside a new group.

Worker universe remains only the old U1.1 paths: package.json, tsconfig.json,
.gitignore, forge.config.cjs, vite.main.config.mjs, vite.preload.config.mjs,
vite.renderer.config.mjs, tools/desktop/prepare-toolchain-deployment.mjs,
tools/harness/validation/run, packages/contracts/xanthil-desktop-ipc.ts,
apps/desktop/main.ts, apps/desktop/preload.ts, apps/desktop/renderer.tsx,
apps/desktop/index.html, apps/desktop/styles.css.
Every group receives the exact smaller behavior/path set; existing IPC changes
only when necessary for admitted wiring and preserve its final contract/GREEN.
No lock/dependency/version/source change; package.json only frozen P5 fields.
Generated build/.vite/out artifacts remain the original untracked paths.

New, separately recorded allowance for this supplement:
- one directed Spec substantive return (1 maximum);
- first Test authoring/validation for each of B1, B2, GUI, serially; within-group
  lawful Test→RED→Worker→GREEN progression is not a retry or a fresh allowance;
- at most two additional correction-and-verification rounds shared by this
  supplement, at most one per root cause, covering test/fixture/control/command
  defects; no ordinary three-round budget stacked on top;
- original Worker/Validator revision and complexity limits continue. Retain the
  prior Worker type revision1; a new group is not permission to reset a repair
  count for the same issue.

Historical Reslice2/2, MapHealth1/1, FOUR, Recovery2/2, Spec returns and UNKNOWN
stay consumed/disclosed. Coordinator pre-registers correction cause, affected
inputs/commands and remaining count; no-progress, exhaustion with a blocker,
unknown effects or outside-scope need stops centrally. Known-safe static reads
use the existing policy, not a new allowance. These two new corrections do not
replenish later U1.2–U4 budgets. Normal first-pass authorized progression continues.

## 5. Intake, acceptance and no expansion

Preserve the latest33 source/copy identities before/after fixed ff-only intake,
not the older31 set. Verify whole diff, file hashes, non-overlap and unchanged
branch/upstream; already received is verified/skipped, already progressed uses
actual current counts/evidence. Drift/conflict stops without reset/stash/clean,
overwrite or early commit. MacBook never writes Mini's branch.

After Spec, coordinator records stage/Gate/ledger and evidence only; it does
not author formal tests/implementation. Test Asset Retirement inventory is
updated at scoped TDD_READY, and the final retirement Gate stays before
independent Validator. U1.1 closes only with actual C1a package/GUI evidence and
all its required leaves; U1 itself still needs real Project/Session/three
directories/save/reopen in U1.2–U1.5. Final91-AC/full-P5/normal-no-debug acceptance,
independent Validator and original final Git delivery gates remain unchanged.

No new product/UI approval, installation, provider/data use, privilege, old
WIP/State/pointer/pause/Ledger/log/worktree/Host Loop/service action, cleanup,
early commit/push/PR on Mini, main integration, archive or release.
Keep Stage0/1, WIP disposition and P1–P4 without replay. Old
CHG-xanthil-desktop-session-bootstrap is historical BLOCKED, not CLOSED.
MacBook publishes only this supplement, readiness review, planning/status/event;
user forwards once to the original task. No automatic send/new task.

## 6. Formal reference set and supplied facts

Authority: [AGENTS](../../../AGENTS.md), [CONTEXT](../../../CONTEXT.md),
[Orchestration](../../../Orchestration.md),
[execution policy](../../governance/product-change-execution-policy.md),
[Git workflow](../../governance/git-development-workflow.md),
[routing](../../governance/agent-model-routing.md),
[complexity](../../governance/change-complexity-control.md),
[reuse](../../governance/xanthil-first-slice-reuse-baseline.md),
[testing](../../../.ai-coding/policies/testing.md),
[done](../../../.ai-coding/definition-of-done.md),
[retirement](../../governance/test-asset-retirement.md),
[architecture](../../architecture/ports-and-adapters.md) and
[security](../../architecture/security-boundaries.md).
Prior frozen execution decisions:
[Reslice001](../2026-09-19/xanthil-desktop-execution-reslice-amendment-001.md),
[Incremental001](../2026-09-19/xanthil-desktop-incremental-contract-amendment-001.md),
[StaticDisposition001](xanthil-desktop-static-diagnostic-disposition-001.md),
[MapHealth001](xanthil-desktop-u11-map-health-disposition-001.md).

For independent plan review, these read-only Mini facts are supplied here; no
external repository lookup is needed. Design M1.1/C1a has no active business
route, Application or Store; Main factory is exactly {senderPolicy}. Public
preload/IPC has twenty final methods/channels and structured-clone results.
Exports are createXanthilDesktopIpcHandlers, xanthilDesktopApi and
XanthilDesktopApp; the normal entry uses these same exports. Renderer takes
exactly {api:XanthilDesktopApi}; component tests may pass another exact
twenty-method implementation. No generic invoke or production test selector.
The prior R/G-U1.1 selects only the IPC contract file; E-U1.1 selects the real
packaged E2E file and previously only allowed post-Worker GREEN. Existing E2E
U1.1 leaves test the persistent frame, six Chinese professional stages, Preview,
mode/search, drawer/modal/focus and 1440x900/1366x768 views. Package launch uses
playwright-core on the same normal arm64 Forge .app, not a Vite browser page.
Approved libraries already include TypeScript, React/ReactDOM, Electron/Forge/
Vite and playwright-core; this supplement adds none. The initial source graph
may not pull in later Application/Store/Pi modules to satisfy a startup import.
Exact field names/config values remain frozen Mini Spec inputs, not invented
here. Spec chooses a concrete supported Test loading mechanism inside the
boundaries above and proves closure before Test; it may not defer that choice
until Worker or treat a missing load method as GUI RED.
