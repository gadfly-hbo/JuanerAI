# U1.1 Spec Consistency Disposition 001

Date: 2026-09-20. Package: PKG-XANTHIL-DESKTOP-001.
Change: xanthil-desktop-membership-repurchase-decision-case.
User authority: the user explicitly authorized the Controller's three-part
disposition: targeted F1/F2 Spec correction with one review-driven rework,
acceptance of the disclosed F3 historical record gap without replay or PASS,
then original progression only after the affected Spec Gate passes.
Readiness: [independent Review001 PASS](reviews/u11-spec-consistency-readiness-001.md),
planning readiness only; this does not replace the affected Spec Gate.
Mini adoption and affected Spec Gate: pending; no role is released by this text
before fixed-commit intake. This is a bounded execution supplement, not a new
Blueprint, UI, product approval or standing-policy change.

Authority references: `AGENTS.md`,
`docs/governance/product-change-execution-policy.md`,
`docs/governance/agent-model-routing.md`,
`docs/governance/change-complexity-control.md`, and
`.ai-coding/policies/testing.md`. Their role, safety and evidence Gates remain.

## 1. Fixed return and retained authority

Continue the original Mini task `纵切-1.0`,
`01a0b45a-13ad-7b92-a2b4-cdde980558e1`, in
`/Users/bendandebaba/JuanerAI`. Mini exclusively owns
`work/macbook/whitepaper-blueprint-v1`, upstream
`origin/work/macbook/whitepaper-blueprint-v1`.
The reported stop HEAD is `02539ed02a22555622b5c61f2a7ddd3b351a8549`,
tree `21fbb6ca87561aa821dae1d91738d7c5bc1d6e51`.
Stop: `U1.1_FIRST_PACKAGE_SPEC_GATE_BLOCKED`.
The current snapshot has 33 files: four tracked modifications, 29 untracked,
no staged changes; eight permitted Spec documents changed in the last return.
Twenty-five other inputs, including all production/tests/config, remain protected.

The original device-local evidence root remains:
`/Users/bendandebaba/JuanerAI-artifacts/xanthil-desktop-membership-repurchase-decision-case/mac-mini-wip-preflight-20260919-001`.
All following evidence names are under its existing `technical-decision-001/`:

| Evidence | Bytes | SHA-256 |
|---|---:|---|
| u11-first-package-gui-admission-stop-index-001.json | 72164 | a0be432ce7bcb73c4522f4b01ca83b5284f967be0b5f8b59f71c376dd91a13a0 |
| u11-first-package-spec-gate-001.md | 9207 | 058b8bed7a3eb8b55b2573e32ae8accd842936ff44a31d5744f3979453ec78ad |
| u11-first-package-spec-role-command-evidence-001.md | 16651 | 1c7cf8e3c39cefb491f791a374f6677ce8d6617c9d72e25682738726ba73f36b |

MacBook read the review and relevant Spec text and matched these evidence
identities. Identity checks are not product or historical-process verification.
Same-device evidence persistence is not a cross-device backup claim.

[First Package / GUI Admission001](xanthil-desktop-u11-first-package-gui-admission-001.md)
remains binding except for the specific F1/F2 closure and extra Spec allowance
below. Its B1/B2/GUI sequence, path universes, causal per-leaf RED, capture,
dependencies and shared Test allowance remain unchanged. Preserve its Spec 1/1
and FAIL, all earlier spent allowances/UNKNOWN, minimum IPC/Main RED→GREEN,
Stage0/1, WIP disposition, tool health and P1–P4 without replay.
The accepted UI, 91 final AC, 20 public requests, schema, Run3.0 and CLI
compatibility are not reopened. Assistance remains nullable as already approved.

## 2. F1 — align GUI Test permission with its required behavior

The stopped `path-contract.md:25–27` and `test-plan.md:148` restrict GUI Test
to health/control/mapping, but the same test plan's command/title table requires
the absent `U1.1 GUI-RUNTIME-SAFETY` behavioral leaf. This is inconsistent
execution wording, not missing user intent; Admission001 already authorizes
applicable real runtime safety on the healthy production package.

Formal Spec must explicitly permit GUI Test to add that named behavior body,
its existing-harness/fixture support and actual coverage mapping within the
already approved GUI Test paths. Preserve the five existing AC-XDESK-001-01..05
titles/assertions and the retained AC-XDESK-012-04 assertion. No assertion is
removed, relabeled as health, or weakened. Test cannot modify production, and a
Worker path list does not grant Test authority. This correction grants no new
security behavior, AC, dependency, product seam or write-path universe.

Closure evidence: the path contract, command/title table, design, Test order
and task/traceability references agree on the same named leaf and its author.
The leaf remains NOT RUN; wording review is neither causal RED nor TDD_READY.

## 3. F2 — distinct evidence identity for normal checkpoints

The stopped plan defines one `H-MAP-U1.1-FIRST-PACKAGE` identity for three
normal first runs after B1, B2 and GUI authoring. Its immutable
`<command-id>/<attempt>/` convention would collide; none has run.

Replace that ambiguous active identity with three concrete command IDs:

- `H-MAP-B1-U1.1-FIRST-PACKAGE`;
- `H-MAP-B2-U1.1-FIRST-PACKAGE`;
- `H-MAP-GUI-U1.1-FIRST-PACKAGE`.

Each keeps the original exact argv, assertion, selected count, environment and
timeout and has its own truthful `first` attempt. Update every active sequence
reference. Preserve historical text/results as history, not active alternatives.
Do not consume a correction for a normal scheduled first run, reuse/overwrite a
directory, invent a fake correction, reset historical ordinals or change the
capture schema. Actual Test/control failures still use the original budgets.

Within this same closure, check the existing B1/B2/GUI schedules for the same
collision at repeated typecheck, GREEN or regression checkpoints. If a second
scheduled invocation would use the same command-id/attempt pair, give that
already-required invocation a checkpoint-specific ID with unchanged argv and
semantics; update the corresponding references. A distinct existing attempt
already preventing collision needs no rename. This is an identity-only repair,
not permission to add a check, rerun successful preparation or build a new
runner, ledger, capture tool or evidence framework.

Closure evidence: a static readable mapping of the already-required scheduled
checkpoints to unique command-id/attempt pairs, including conditional safety/UI
branches. No test/build/package command runs to prove this textual property.

## 4. F3 — accepted, bounded historical evidence limitation

The user accepts the disclosed residual availability gap for the completed
Admission001 Spec return identified by the command-evidence document above.
It covers missing full early static search/path records, an apply_patch
context-mismatch payload, the exact expanded no-index comparison arguments,
a truncated listing, and nested numeric exits not retained. Unified tool output
cannot be reconstructed into separate stdout/stderr; compaction summaries are
not raw transcripts. Keep those distinctions, failures and UNKNOWN intact.

The accepted limitation no longer blocks this specific resumption. It does not
prove that every historical effect was absent, approve an unobserved operation,
waive a product assertion or convert any old role/Gate/command verdict to PASS.
Current file/hash/scope checks establish the present snapshot only. Do not
replay lost historical commands or manufacture missing output to close F3.
Any newly discovered different effect, protected-input change or evidence gap
is outside this disposition and follows the existing stop line.

For new work, retain original available native tool records before context
loss in the existing evidence root. Distinguish shell numeric exits from
non-shell tool success and unified output from separated streams. Do not
invent shell fields for apply_patch. Required test/build/GUI execution still
uses the approved complete capture; no prospective evidence waiver is granted.

## 5. Directed role, budget and Gate

After verified intake, Mini dispatches a fresh formal `juaner_spec`, configured
`gpt-5.6-sol`, explicit R2 `xhigh`, for F1/F2 and consistent stage recording only.
R2 reason: test authority/security verification and repeated execution-contract
closure. Confirm role/effort availability before dispatch; record requested
settings and any effective-runtime echo limitation, never invent runtime proof.
Override duration is this bounded return/rework; subsequent tasks use their
applicable route. Missing authority is not solved by a stronger model.

Under the existing Change directory, Spec may edit only `proposal.md`,
`design.md`, `test-plan.md`, `path-contract.md`, `dependency-decision.md`,
`tasks.md`, `traceability.md`, `verification.md`; touch only needed clauses,
not all eight by default. Both Requirement files, structure-decision,
decision-brief and all production/tests/config/manifest/lock remain byte-frozen.
Coordinator edits only the existing three stage records plus original-root
evidence, and never substitutes for Spec/Test/Worker/Validator.

New allowance, separate from Admission001 Spec 1/1:

1. One targeted substantive Spec correction return.
2. At most one review-driven Spec rework if that return leaves a concrete
   in-scope F1/F2 consistency finding; it is not automatic or a new design round.

Record these separately. A substantive BLOCKED return still counts; ordinary
safe static reads, progress updates and read-only clarification are not extra
substantive returns under the existing policy. No-progress, unknown effects,
scope expansion or a remaining blocker after this allowance stops centrally.
Do not force a consumed return merely to inspect a corrected safe read error.
Historical budgets are neither reset nor retroactively approved.
New B1/B2/GUI Test correction remains **0/2 unused**, with the original shared
limit and one-per-root-cause restriction; this Spec work consumes none of it.
No ordinary correction allowance stacks on top to extend these explicit limits.

After each substantive return, Mini reviews the full twelve-file package and
complete current diff, performs required ponytail review and decides the
affected Spec Gate against F1/F2 closure, protected-input integrity and this F3
disposition. No automatic PASS follows user approval or a Spec READY heading.
Only PASS releases the original B1→B2→GUI progression; each Worker still needs
scoped healthy causal RED/TDD_READY, with original regression, retirement,
independent Validator and final acceptance obligations intact.

## 6. Publication, intake and return point

MacBook publishes only this Controller supplement, its review and the existing
planning/status records on its own branch. It does not write Mini's branch or
dispatch production roles. The user manually forwards the fixed-commit handoff
to the original task; no new task or automatic message is authorized here.

Before one ff-only intake, Mini checks the supplied commit, unique parent,
tree, complete diff and exact file identities; proves no overlap with all 33
stopped sources; matches those sources/copies to the latest stop index before
and after intake. Already adopted is verified and skipped; already progressed
uses actual evidence/counts, not a replay. Drift, conflict or missing evidence
stops without reset, stash, clean, overwrite or premature commit.

Do not repeat old WIP/privileged checks, installs, successful preparation,
Host Loop/service operations or lost historical commands. No real data/provider,
host privilege, cleanup, new dependency or Git-delivery permission is added.
The old CHG-xanthil-desktop-session-bootstrap remains historical
BLOCKED / DISPATCH_ORPHAN_READY, not CLOSED. This product Change is not accepted.
Return only a consolidated exception if the bounded closure fails; otherwise
continue the originally admitted stages without per-Gate user forwarding.
