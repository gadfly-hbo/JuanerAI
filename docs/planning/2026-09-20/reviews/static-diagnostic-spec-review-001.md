# Static Diagnostic Disposition 001 — Controller Spec Review

## Verdict and scope

On 2026-09-20 the MacBook Controller completed the authorized read-only review
after the user's separate acceptance of the disclosed MacBook incident evidence
gaps. **Affected Spec Gate: PASS for the exact frozen package below.** No
executable product validation, Test/RED, TDD_READY or acceptance is claimed.
The original Mini execution-overrun BLOCKED and interrupted MacBook support
review remain unchanged. This verdict comes from resumed Controller review,
not from the interrupted support Agent or the accidental test run.

Package: `PKG-XANTHIL-DESKTOP-001`; Change:
`xanthil-desktop-membership-repurchase-decision-case`; Mini task:
`纵切-1.0 / 01a0b45a-13ad-7b92-a2b4-cdde980558e1`.
Mini HEAD remains `f2cd897a6829ce6879d12057a3cf8e42b0708a08`, with the
same Mini-owned branch/upstream and four tracked/twenty-seven untracked assets.

## Reviewed identity and method

- Current index: `interrupted-spec-final-001.json`, 130733 bytes,
  SHA-256 `a84e15dfbc5399b89fa9504dfdef6ea654d67a7f9c132afd78c9c7cbeb8daae1`.
- Previous frozen index: `directed-spec-final-001.json`, 94089 bytes,
  SHA-256 `5212d1cb9bd5f87ee88a5da0aa7351a1570b4a111713cc12be3cc1e03298a694`.
- Fresh Mini SHA-256 readback matched all31 current sources and all31 frozen
  copies. Both twelve-document local copy sets match their respective index.
- All twelve document deltas were reviewed, with focused reading of affected
  design, command, path and task context. The Mini's retained whole-package
  review is reused for unchanged bodies; this does not claim a new line-by-line
  reread of every historical stage paragraph or repetition of all174 checks.
- Text-only equality checks confirm both Requirement bodies, the complete
  structure body, twenty public-request rows, five-prefix table and the entire
  dependency-decision file unchanged. No product code was imported or run.

The local comparison initially used the current index's older `before` fields
for four previous-copy identities and reported mismatches. One read-only
correction used the explicitly named previous index's `source/copy` identities;
all twelve matched. The failed comparison is retained, not relabeled PASS.
No input, assertion, file or product operation changed during that correction.

## F1–F6 findings

| Item | Current disposition and source within the frozen Change |
|---|---|
| F1 | PASS — design.md:71–88 and :782–790 agree: sender/frame, method-specific version/command envelope, inactive refusal or active full validation. Six non-command requests retain their shapes. |
| F2 | PASS — design.md:98–167 uses the real existing Store list/read chain, stored Project scope and Session/Case/current-revision identities. Missing/stale/integrity/read-error and receipt mappings are explicit; no invented owner field, resolver, cache or Port. |
| F3 | PASS, retained — design.md:37–49 keeps the five exact source prefixes and separates public, Application, Store and dependency closure. No runtime phase selector is introduced. |
| F4 | PASS — test-plan.md:88, :122–142 and tasks.md loop rows require latest GREEN U1.4/U3.2/U4.2 packages before their corresponding E2E REDs, and fresh target packages before post-Worker E2E-bearing GREEN. Missing package is health failure. |
| F5 | PASS — test-plan.md:84–98, :144–166, :651–690 and path-contract.md:34–40, :194–207 separate anchored health from selected top-level CF contract leaves. Already-correct XCLI contract may be GREEN; the focused-to-full runner supplies causal RED. Test/coordinator, not Worker, owns test-oracle retirement. |
| F6 | PASS — test-plan.md:39–112 freezes absolute executables, exact cwd, ten-field environment and concrete argv expansion. Active sequences refer to command IDs; historical command examples are not alternative execution authority. |

Proposal/decision status headers, task ordering, path ownership, traceability
and verification preserve pending downstream Gates and prior failure history.
The two spec files and structure-decision change only author-status text;
dependency-decision is byte-identical. No product/UI, final91AC, Run3.0,
dependency permission, compatibility or schema expansion was found in this delta.

## Complexity review

Applied `ponytail-review` to the complete twelve-file delta and its affected
context. F1/F2 serve real U1 admission/ownership; F3 is retained; F4 serves
causal same-app GUI evidence; F5 serves already-approved final restoration and
role isolation; F6 removes competing execution instructions. All map to the
approved first-slice consumer and acceptance obligations. No new runtime,
service, retry engine, registry, persistent protocol or enterprise behavior.

Lean already. Ship.

That sentence is only the skill's complexity result, not shipping permission.

## Evidence and release boundary

Original Mini root remains the existing Change artifact root, under
`technical-decision-001/`. Local source copies and fresh readback are under
`/Users/huangbo/JuanerAI-artifacts/PKG-XANTHIL-DESKTOP-001/dependency-structure-decision-20260919/incremental-contract-disposition-001/static-spec-review-resume-001/`:
`current/`, `previous/`, `interrupted-spec-final-001.json`, and
`controller-readback-001.json`. These are MacBook evidence copies, not another
Spec authority. The original incident JSON/npm log remain in the parent root.

This Gate applies only while those frozen normative inputs are unchanged.
The original Mini task must verify the published supplement's fixed identity,
non-overlap and unchanged31 assets, then append this disposition/current Gate
to its existing three stage records without erasing BLOCKED history. It may
then dispatch fresh formal Test for U1.1 under the supplement. No new Spec
return, budget reset, early Worker/Validator, successful-check replay, cleanup,
old-state/service operation or real Provider call is authorized.
