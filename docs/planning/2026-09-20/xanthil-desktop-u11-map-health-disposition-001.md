# PKG-XANTHIL-DESKTOP-001 — U1.1 Map Health Disposition 001

## Decision and exact return point

On 2026-09-20 the user authorized one directed Test correction and verification
for the multiline manual-acceptance marker mismatch, then continuation of the
three not-yet-run U1.1 commands if health passes. This R0 Controller supplement
changes no product, UI, architecture, Spec, dependency or standing policy.

Mini adopted `434e8fd810c2e19953f084b67ddfaf465f0f2ae8` and retained the
Controller's affected Spec Gate PASS. It stopped at
`STATIC_DIAGNOSTIC_U1_1_TEST_HEALTH_BLOCKED_BUDGET_EXHAUSTED`.
The prior Reslice extra-Test allowance is **2/2 consumed, zero remaining**.
Keep the coordinator environment-capture mistake and Test mapping correction,
all older FOUR/Recovery2/2/Spec1/1, failures and UNKNOWN unchanged.
This grants exactly **one additional targeted correction/verification round**,
recorded separately as Map Health Disposition001 0/1 until used. It does not
reset, stack or replenish an ordinary/general correction allowance.

The verified root cause is `tests/unit/xanthil-desktop/coverage-map.test.ts:44`:
its literal phrase requires a space after `native-chooser`; the retained E2E
comment continues on the next `//` line. `AC-XDESK-012-05` and its post-GREEN
manual/no-debug acceptance obligation are present and remain NOT RUN. The last
H-MAP child exited1, capture exited0; this is test-health failure, not product RED.

## Frozen identity and intake

- Original task: `纵切-1.0 / 01a0b45a-13ad-7b92-a2b4-cdde980558e1`.
- Mini repository/device: `/Users/bendandebaba/JuanerAI` / `mac-mini`.
- Keep branch/upstream `work/macbook/whitepaper-blueprint-v1` /
  `origin/work/macbook/whitepaper-blueprint-v1`, exclusively Mini-owned.
- Intake parent: `434e8fd810c2e19953f084b67ddfaf465f0f2ae8`;
  tree `00b53c41117bee15aab4612681683c2e602a7522`.
- Publication branch: `work/macbook/wip-preflight-exception-disposition`.
  The fixed publication receipt supplies commit/tree/parent and changed-file
  hashes; never substitute a future branch tip.

The existing Mini root remains
`/Users/bendandebaba/JuanerAI-artifacts/xanthil-desktop-membership-repurchase-decision-case/mac-mini-wip-preflight-20260919-001`.
Use `technical-decision-001/static-diagnostic-u11-stop-index-001.json`,
192478 bytes, SHA-256
`7f9a62a0f0fb1ba0966ec2c8bba439bce4ad730ba8373416959127362662094d`,
as the latest31-source/31-copy intake baseline, not the previous Spec stop index.
MacBook freshly matched all62 hashes and inspected the mismatch source and
last command record; this is not a new execution or test PASS.

Verify the fixed supplement, its complete diff/file identities, non-overlap and
31 WIP identities, then perform one ff-only intake on the same branch. Read back
31 unchanged WIP files and preserve upstream. Already received: verify and skip
merge. Already progressed: use current evidence/counts rather than redispatch
or replay. Drift/conflict stops without reset, stash, clean, overwrite or early
commit. Retain all unused command envelopes and original failed output.

## One directed formal Test correction

After intake, resume the existing formal `juaner_test` assignment with requested
R2/xhigh and its configured model. Reuse its retained context when available;
otherwise give a fresh invocation the frozen assignment, stop evidence and this
same remaining allowance. This is not a new task, Spec return or Test restart.
Record what routing the platform actually exposes, not an inferred runtime tuple.

During this correction, Test may modify only
`tests/unit/xanthil-desktop/coverage-map.test.ts`, limited to the existing manual
marker match and minimal same-file checks of that match. Accept the same exact
AC and full phrase on one line or wrapped across adjacent line comments; do
not accept a missing/wrong AC or a missing/truncated obligation. Small literal
positive/negative cases in this same health test are permitted; they need no new
file, parser framework or extra command. All other health assertions,91 AC/map
entries, business test bodies and the E2E manual comment remain unchanged.
Do not turn manual-post-green/NOT RUN into automated coverage or acceptance.

The coordinator may update only existing stage records
`verification.md`, `traceability.md`, `tasks.md` and append command/input/evidence
records under the original artifact root. It does not author the Test repair.
Nine normative Spec files, production, manifests/lock, other tests and fixtures
remain frozen during this correction. No new capture tool or environment repair:
reuse the already-corrected capture behavior, passing exactly the approved ten
environment fields; retain the disclosed macOS-added encoding metadata separately.

## Exact bounded verification and continuation

Freeze the changed input, then run these existing command IDs once, in order:

1. `H-SYNTAX-U1.1-5`: approved absolute Node with argv
   `--check`, `tests/unit/xanthil-desktop/coverage-map.test.ts`.
2. Only after syntax exit0, `H-MAP-U1.1`: the same Node with argv
   `--experimental-strip-types`, `--test`,
   `--test-name-pattern=Desktop test fixture and 91-AC coverage map health`,
   `tests/unit/xanthil-desktop/coverage-map.test.ts`.

Use the frozen test-plan's exact repository cwd, absolute tool root, ten-field
clean child environment and180000ms limit. Preserve full argv/input hashes,
start/end, separate stdout/stderr, numeric child/capture exit and readback.
Record the two attempts under the existing
`technical-decision-001/incremental-contract-execution-001/U1.1/<command-id>/map-health-disposition-001/`.
This supplement explicitly permits that new attempt label only for these two
commands; it prevents overwriting first/correction-1/correction-2 history.
One corrected return with this verification sequence consumes the sole round,
including failure. Stop at the first failure; no further correction or rerun.

After both pass, retain the new health evidence and continue the original
unexecuted `H-TUPLE-U1.1`, then `H-RUNNER-U1.1`, then `R-U1.1` in order, stopping
on invalid health/evidence. Reuse their retained argv/environment; freeze any
changed input identities in appended records before execution, without changing
argv, assertions or scope. Their retained `correction-2` directory names do not
mean they ran: preserve `actual_execution_ordinal_for_id=1` and all original
unused envelopes. If a current input needs a new envelope, append a non-overwriting
`map-health-disposition-001` attempt with that same actual first ordinal; this
label is also allowed for these three IDs only. Do not rerun the other successful
syntax commands or previously successful preparation.

H-MAP PASS proves map/fixture health only, never91 behavior coverage. Formal Test
must still return its required asset inventory, assertion mapping and genuine
U1.1 RED evidence; Controller/delegated coordinator must actually pass scoped
TDD_READY before separate Worker. A missing tool/app/raw import failure is not
product RED. Successful RED→Worker→GREEN and later already-approved first-pass
loops retain their original authority under
[Static Diagnostic Disposition001](xanthil-desktop-static-diagnostic-disposition-001.md).
No new general Test-correction budget is created for later failures. No per-Gate
user relay is needed for normal authorized progress.

## Preservation and endpoint

Append disposition/count/Gate records; do not erase the prior stopped verdict
or reapprove unchanged Spec/product/UI. Keep Stage0/1, WIP_DISPOSITION_COMPLETE,
tool health and P1–P4 without replay. No new install, provider/data, host/sudo,
service, old State/pointer/pause/Ledger/log/worktree/Host Loop, cleanup, main,
integration, archive or release authority. Old
`CHG-xanthil-desktop-session-bootstrap` remains historically BLOCKED, not CLOSED.

MacBook publishes only this supplement, planning/status and its event record,
then stops writing its publication branch. User forwards the fixed receipt to
the original Mini task; no automatic send, new task or MacBook production-role
dispatch. The next receipt is normal authorized delivery or a genuine remaining
blocker, not a request to reapprove product intent.
