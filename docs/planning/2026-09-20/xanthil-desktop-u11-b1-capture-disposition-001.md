# U1.1 B1 Capture Disposition 001

Date: 2026-09-20. Package: PKG-XANTHIL-DESKTOP-001.
Change: xanthil-desktop-membership-repurchase-decision-case.
User explicitly authorized this limited H2 evidence-risk disposition, use of
the one remaining correction, file-based capture transport and continuation
from H2 without replaying H1 or reopening Spec/product approval.

Classification: R0 support / execution correction under
`docs/governance/change-complexity-control.md` and
`docs/governance/product-change-execution-policy.md`. This is no new/material
product plan, assertion, schema or capture framework. Controller scope/evidence
review applies; no new Product Plan Reviewer or Spec return is required.
[Admission001](xanthil-desktop-u11-first-package-gui-admission-001.md) and
[Spec Consistency001](xanthil-desktop-u11-spec-consistency-disposition-001.md)
remain binding except for this exact transport/resumption disposition.

## 1. Stopped identity and accepted evidence limit

Continue only `纵切-1.0` / `01a0b45a-13ad-7b92-a2b4-cdde980558e1`,
Mini repo `/Users/bendandebaba/JuanerAI`, branch
`work/macbook/whitepaper-blueprint-v1`, upstream
`origin/work/macbook/whitepaper-blueprint-v1`.
Stop HEAD `a4ab8d9b5405d51e39b66d069edee143d3e72ede`, tree
`46e7055cd85406193cb3f6c3ba17ad06e91f190d`.
State: `U1.1_B1_TEST_BLOCKED_NEW_OUTER_EXIT_UNKNOWN`.

Evidence remains under the original Mini root:
`/Users/bendandebaba/JuanerAI-artifacts/xanthil-desktop-membership-repurchase-decision-case/mac-mini-wip-preflight-20260919-001`.
The following files are under its `technical-decision-001/` child:

| File | Bytes | SHA-256 |
|---|---:|---|
| u11-spec-consistency-stop-index-001.json | 52929 | 7431d0d987cc65128c7fca9420302264bc69c33527a56c6d20285cdccf149199 |
| u11-spec-consistency-gate-001.md | 8480 | eb9b88d5fc7e3918391b888aecda310f8b4da56851ceed09ee4650cfa6d516c4 |
| u11-b1-first-command-freeze-001.json | 15219 | 46e391f6eae48f08cf6c1347e1e6788f5819abe6c7695f60cfe57957ead866c9 |
| u11-b1-test-role-phase-b-stop-001-native-export.jsonl | 15712 | a9291f714808e8bbfdb04d19843558bd856f305e9702cf203cf001f5ee6978fe |

MacBook read the native calls/error, command freeze, Gate and H1 result, and
matched 33 current sources, 33 frozen copies and five key evidence identities.
This establishes a current snapshot, not absent historical effects or product
verification. Mini's affected Spec Gate PASS is retained, not independently
re-performed by these hashes. New behavior RED/TDD_READY does not yet exist.

The H2 native record shows outer Node source parsing failed before capture.
Its numeric tools.exec_command exit was not retained because only output was
emitted. The user accepts this one disclosed missing value as UNKNOWN; neither
SyntaxError nor Script completed supplies a numeric value. Keep original calls,
errors, H2 first envelopes and the noReplay boolean correction intact. Do not
replay to reconstruct history or claim H2 was a product RED. This specific gap
no longer blocks this resumption; a different/new unknown still stops.

## 2. Single transport correction; unchanged capture semantics

Use the existing `capture_source` string in the frozen command document above.
Its decoded UTF-8 source is 4005 bytes, SHA-256
`132d7a9c90f4f15098c9563e484e3dc8303bb5b2dad5154528f65d15e4359384`.
Decode JSON once to actual source text; do not put JSON.stringify(source) into
a shell command, convert its actual LF to literal backslash-n, or paste the
source into another multi-layer `node -e` string.

Materialize one exclusive, non-overwriting `.cjs` file in the original evidence
root at `technical-decision-001/u11-b1-capture-file-001.cjs`. File execution
adds the script path at argv[1], so the sole permitted source adaptation is:

```text
old: const directory=process.argv[1],expectedCommand=process.argv[2],expectedInputs=process.argv[3];
new: const directory=process.argv[2],expectedCommand=process.argv[3],expectedInputs=process.argv[4];
```

Require exactly one original-line match; add one final LF for the file form
(the original string has none). No other source change is permitted. The
resulting file is 4006 bytes, SHA-256
`7e2e4c8eb8265da3f358d0c135f9cedc577b2e8447b3baba1315ec86b948d60c`.
Preserve the original freeze; record source/file bytes and SHA-256, the exact diff,
and independent file readback before execution. If the path already exists,
verify and reuse only the exact approved derivative; never overwrite it.
This mechanical argv adaptation preserves capture/validation logic, timeout,
environment checks, child spawn, output/result format and assertions.

Invoke the original approved absolute Node with the file's absolute path,
new attempt directory, command SHA and inputs SHA as four arguments after Node.
Use the same approved command-local environment/cwd as the frozen outer launch,
including the existing macOS encoding-metadata treatment. The business/check
child argv and exact ten-field child env remain unchanged. No ambient node,
new flag, loader, dependency, capture schema or replacement harness is allowed.

Retain the actual complete native tool-return object, not only its output.
Keep any yielded session identifier, wait for actual completion and preserve
its numeric exit/signal and all output chunks. Retain the original call too.
The approved capture separately preserves child stdout/stderr/result and
interpretation; an outer failure never becomes child PASS or RED. Do not
invent separate streams or exits absent from a tool response. Use existing
evidence records; no new durable capture system or retry wrapper.

## 3. Fresh envelopes and the exact return point

Before intake, match the latest33 sources/copies above, not an earlier stop.
Verify fixed Git commit/parent/tree/full diff/each file and no overlap, then
one fixed ff-only intake; recheck33 afterwards. Keep branch/upstream unchanged.
Already-adopted is verified/skipped; already-progressed uses actual counts.
Unexpected drift stops without reset/stash/clean/overwrite or early commit.

Freeze a new non-overwriting envelope set before running. The old inputs include
stage records subsequently updated for the stop; do not blindly reuse their
hashes or rewrite original envelopes. Account explicitly for the authorized
three stage records and this fixed Controller intake, bind current identities
and retain the before/after delta. All other recorded inputs, especially the
five B1 Test assets and production/spec/tool/dependency inputs, must match their
approved baselines. Any unexplained difference stops; refreshing a hash cannot
approve it. Snapshot stage records before the serial batch and record its
results after return; a later authorized stage write requires another truthful
non-overwriting freeze, not silent hash replacement.

Use the original `.../incremental-contract-execution-001/U1.1/` root. For just
the seven commands below, append `-RESUME-001` to the old command ID so old
command/input/output directories remain intact. No child argv/count/timeout/
assertion changes. Preserve origin-ID mapping and actual ordinal in existing
stage/interpretation records; no capture-schema field is added.

| Original command ID, in order | New attempt under its suffixed ID | Meaning |
|---|---|---|
| H-SYNTAX-B1-U1.1-2 | correction-2 | second outer attempt, first actual child syntax check; historical failure stays |
| H-SYNTAX-B1-U1.1-3 | first | not previously run |
| H-SYNTAX-B1-U1.1-4 | first | not previously run |
| H-SYNTAX-B1-U1.1-5 | first | not previously run |
| H-MAP-B1-U1.1-FIRST-PACKAGE | first | not previously run |
| H-B1-LOAD-U1.1 | first | not previously run |
| R-B1-BRIDGE-U1.1 | first | not previously run; classify actual reached assertions |

Retain H-SYNTAX-B1-U1.1-1/first HEALTH_PASS when its checked source/tool is
unchanged; do not rerun it because stage metadata changed. Its retained result
is 3745 bytes, SHA-256
`176fc892be570eae5ff455c58c213168136d7ea73877a3e92639b30675295e7e`.
Health must pass serially before the next command. Any unexpected health,
capture, input or evidence failure stops, not a product RED. Test returns after
R-B1-BRIDGE; the coordinator may then admit only the original leaf-scoped
Worker path after genuine causal RED and scoped TDD_READY, or retain legitimate
baseline GREEN without inventing RED. Original downstream progression remains.

## 4. Ownership, accounting and stop line

Only Mini resumes the formal `juaner_test` role, configured gpt-5.6-terra,
requested R2/xhigh under existing routing. Reuse its stopped context if available;
otherwise a fresh same-role context inherits all frozen inputs/counts, not a new
allowance. Controller/coordinator never substitutes for Test or Worker.
Test may materialize the exact capture derivative and new envelopes/results in
the original evidence root, but may not revise any test, fixture, assertion,
product, dependency or normative Spec under this correction. Coordinator owns
only the existing verification/traceability/tasks stage records and evidence.

Pre-register this transport correction against the existing final allowance:
Admission correction1/2 is already used; this correction plus H2 verification
consumes correction2/2 even if it fails. There is no new or stacked allowance.
Later listed first runs and lawful per-leaf RED→Worker→GREEN are original
progression, not new correction grants. No automatic retry after an unexpected
failure, exhausted correction budget or unknown effect. All historical quotas,
failures, UNKNOWN and Worker revision1 remain; unused Spec rework is not Test
budget and does not authorize reopening Spec for this issue.

MacBook publishes this short Controller disposition plus the existing planning/
board records; the user forwards the fixed receipt to the original Mini task.
No new task, automatic message, MacBook product-role dispatch or product acceptance.
Keep original WIP disposition, Stage0/1, tools, P1–P4 and minimum IPC GREEN.
No historical replay, install, provider/real data, privileged/host/old state or
Host Loop operation, cleanup or new Git-delivery authority. Old bootstrap Change
remains BLOCKED / DISPATCH_ORPHAN_READY, not CLOSED.
