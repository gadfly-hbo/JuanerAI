# B2 Test Contract Disposition 001

Date: 2026-09-20. Package: PKG-XANTHIL-DESKTOP-001.
Change: xanthil-desktop-membership-repurchase-decision-case.

## Decision and identity

The user approved this consolidated disposition after Controller read-only
review: retain scoped B1 completion; correct B2 Test against the existing
contract; add two shared B2 Test-content correction rounds; extend bounded
restoration of frozen invocation values to B2; accept only the specifically
disclosed historical evidence limitation below. No product/UI/Spec decision
changes. This is R0 execution-authority documentation for the existing R2
foundation Change, not a new product plan or a Spec/RED/acceptance PASS.

Continue only task `纵切-1.0` / `01a0b45a-13ad-7b92-a2b4-cdde980558e1`,
Mini repo `/Users/bendandebaba/JuanerAI`, branch
`work/macbook/whitepaper-blueprint-v1`, upstream
`origin/work/macbook/whitepaper-blueprint-v1`.
Stop HEAD `5f05bd1dbeebecbf166b14892d263129330e4765`,
tree `694c1979ff10315c5c0f871697ad500a4cb237b0`.
MacBook publishes only on `work/macbook/wip-preflight-exception-disposition`;
the user manually forwards the fixed receipt to the original task. No automatic
message, new task, branch switch or Mini production execution by MacBook.

Mini's sole evidence directory remains:
`/Users/bendandebaba/JuanerAI-artifacts/xanthil-desktop-membership-repurchase-decision-case/mac-mini-wip-preflight-20260919-001/technical-decision-001`.

| Preserved file in that directory | Bytes | SHA-256 |
| --- | ---: | --- |
| u11-b1-frozen-invocation-b2-stop-index-001.json | 194238 | f65bcb164174d2df6ed86b70b4a9edfb31fcb366f3f649289ea7f1d561f0855f |
| u11-b1-frozen-invocation-b2-readback-001.json | 116656 | db15f4ef5d305235a19c395c329af724cb8992bf238660b260cf40ff3884d237 |
| u11-b1-completion-gate-001.json | 131542 | b63203a97b62dae99816d1b2d91b9cbcdf268adc18ca6312900ee1472f9a4b5e |
| u11-b2-original-records-001.json | 8880113 | 22fe7a7d4238601caa7cc7c80bc8d517dfe889f1331bb873008555c8623f6995 |

MacBook read the actual design and affected Test bodies, the formal BLOCKED
return, Root review and B1 Gate, and independently matched the current 34
source files and 34 final copies to the stop index. This is bounded scope and
identity review, not replay of B1 or full independent product verification.
Controller intake evidence is on MacBook at
`/Users/huangbo/JuanerAI-artifacts/PKG-XANTHIL-DESKTOP-001/dependency-structure-decision-20260919/incremental-contract-disposition-001/b2-test-contract-disposition-001/controller-intake-001.json`.
Mini originals remain accessible by their locators; they are not thereby
cross-device backups. B1 and B2 product/Test assets remain uncommitted.

## Retained verdict and historical disposition

Retain B1 Bridge/Entry/Safety scoped RED -> independent Worker -> GREEN and
retained IPC regression. The IPC top-level selected/executed/passed count is
one, with 114 internal observations across 20 methods, not 114 top-level tests.
This is not packaged security, GUI, final 91-AC, retirement, Validator or product
acceptance. B2 remains TEST_CONTRACT_BLOCKED until this fixed intake and repair.
The recovered command record and unused eight first envelopes record B2 as
NOT RUN; the 24 empty output slots alone are not execution proof. None is RED,
TDD_READY or a Worker release.

The B2 envelope quotation repair exceeded its applicable exhausted allowance.
Keep that deviation and the actual one correction; do not retrospectively grant
it authority or reset Admission2/2. The user accepts only the disclosed record
limitation associated with `u11-b2-original-records-001.json`: original calls,
53 backend commands/results and the five-file patch were recovered, but original
un-emitted API chunk/session/wall-time objects, ambient shell environment and
independent historical stderr separation cannot be recovered. The four exit-1
events remain wc-directory, symlink-read misclassification, rg no-match and
shell quotation failure, not product RED.

Retain UNKNOWN and original failures. This limited acceptance permits prospective
continuation; it proves neither every historical side effect absent nor a test
PASS. Do not replay commands to invent lost evidence. Newly unknown effects,
new missing execution evidence or a contradiction outside this precise record
are not waived. Use the existing capture and complete native tool returns for
new execution, including yielded completion; do not emit only `r.output`.

## Formal Test correction scope

After fixed intake, dispatch fresh formal `juaner_test`, configured model,
R2/xhigh, with the current twelve-file package read-only. Do not dispatch Spec.
Test may revise only these five existing assets, only their B2 portions and
directly necessary shared-loader/control or mapping support:

- tests/contract/xanthil-desktop/xanthil-desktop-ipc.contract.test.ts
- tests/fixtures/xanthil-desktop/desktop-contract-drivers.ts
- tests/fixtures/xanthil-desktop/desktop-fixtures.ts
- tests/fixtures/xanthil-desktop/coverage-map.ts
- tests/unit/xanthil-desktop/coverage-map.test.ts

The coordinator may update only the existing Change's verification.md,
traceability.md and tasks.md, plus append non-overwriting evidence under the
original root. All other current 34-file inputs stay frozen through this Test
repair. Keep the nine Spec bodies, 91 final AC, twenty public methods, schema,
Run3.0, dependencies/lock, B1 product bytes, CSVs and future tests unchanged.
The later lawful Worker phase retains its separately frozen original paths;
this Test allowance does not itself open any production path.

| Finding / current source location | Required closure against unchanged design |
| --- | --- |
| F1: IPC contract test 401-403 | Remove the contradictory permanent requirement that COMPONENT capture must always be empty. Prove the real exported component's initial markup and zero business API calls; permit its normal mount when rerun after MOUNT. MOUNT still owns exact root/createRoot/render assertions. No production test flag or premature mount Worker scope. |
| F2: drivers 518-535 | Use the pinned transpileModule contract, including explicit isolatedModules:true, exact source bytes, a fresh command-evidence .mjs, and source/emitted byte lengths and hashes. Import that emitted file, not a data URL for the Renderer. Retain positive compile/render and negative diagnostic controls. |
| F3: drivers 450-466, fallback462 | Reject unapproved bare/runtime-relative imports originating in the emitted module. Preserve the approved real React/jsx-runtime resolution, actual react-dom/server and sole DOM-mount substitute react-dom/client. Do not globally prohibit valid installed-library internal imports. |
| F4: IPC contract test 407-421 | Assert the actual index has one root, only relative local script/style resources and the exact existing CSP in design.md. Retain the identical actual component/API mount and zero business API call checks. |

Read design.md's B2 loading and COMPONENT/MOUNT sections in full; line numbers
above locate the received version, not a substitute for the contract. Preserve
the original role's mislocated F3/F4 report with its recorded correction.
Selectors were correct: do not repair an invented selector defect. No new loader
framework, capture tool, dependency, alternate app or product test entry.

## Two distinct prospective budgets

**B2 Test-content: two shared correction-and-verification rounds, initially 0/2.**
The first consolidated F1-F4 revision consumes round1; there is no free first
authoring after the existing BLOCKED return. Round2 is available only for
remaining in-scope Test/control findings with demonstrated progress. The four
findings do not each receive two rounds. These two rounds override only the
exhausted Admission correction cap and its one-per-root-cause restriction for
this B2 Test repair. They do not replenish GUI or U1.2-U4 corrections. Stop if
no progress, effects unknown, scope changes or a blocker remains after round2.

**B2 invocation restoration: at most three corrections per actual root cause.**
Prospectively extend the [B1 frozen-invocation rule](xanthil-desktop-b1-frozen-invocation-disposition-001.md)
to authorized B2 commands and their task-local envelope preparation. Restore
actual arguments, quoting, paths and environment to approved frozen values;
derive and compare them from the frozen command rather than retyping paths.
Effects must be understood and repetition safe. Keep the capture checks,
approved absolute tools, cwd, ten-field child environment and 180000ms bound.
An actual failed health/assertion is not invocation restoration; its Test
revision consumes the two-round content allowance instead.

Counts remain cumulative across commands/sessions/amendments; do not reset or
rename a cause. Include the disclosed one B2 quotation correction when the same
cause recurs, without retrospectively authorizing it. Similar error wording
alone is not proof of a shared cause. Admission2/2, preparation2/3, resolved H3
1/3, prior Worker revisions and all earlier FOUR/Recovery/Reslice/Spec histories
remain. No transfer of the unused B1 preparation round. Apply existing
[static-reading and mistaken-pause distinctions](../../governance/product-change-execution-policy.md#execution-accounting-and-mistaken-pauses)
without excusing substantive Test changes. Use existing stage records, no ledger
or retry framework. Register cause, affected inputs, commands, count and result.

## Exact return path and closing condition

1. Verify the publication receipt's fixed commit/tree/unique parent, full diff,
   every changed-file hash and no overlap with the latest 34 current files.
   Match those files and final copies before/after one ff-only intake; keep
   branch/upstream. Verify-and-skip already received intake. Preserve drift and
   stop; no reset, stash, clean, overwrite or early commit.
2. Register B2 Test round1 and close F1-F4 together. Preserve the old five-file
   version and unused eight envelopes; bind corrected inputs in fresh evidence
   children using the existing command mechanism. Input hashes may change only
   for the five authorized Test paths and three stage records. Refreshing a hash
   never grants a contract/command change. Preserve actual execution ordinals:
   the original B2 eight commands were NOT RUN, not failed tests.
3. Execute the existing test-plan.md B2 sequence: affected syntax -> mapping ->
   TSX positive/negative loader health -> R-B2-COMPONENT. Record full commands,
   environments, outputs, numeric exits, input identities and selected counts.
   Restore any affected B1 loader/control/regression evidence required by shared
   fixture changes, using only existing applicable commands. Do not replay
   unchanged successful preparation, WIP, P1-P4 or the old B1 RED.
4. Only actual missing behavior under healthy tests admits the original scoped
   TDD_READY and a separate formal Worker. Continue serial COMPONENT GREEN ->
   MOUNT RED/Worker/GREEN -> COMPONENT regression -> complete C1a health/package
   -> existing GUI admission, under the original [Admission001](xanthil-desktop-u11-first-package-gui-admission-001.md)
   and current frozen Spec. A passing leaf needs no Worker; a failure behind an
   unreached leaf is not its RED. This adds no GUI correction or Worker allowance.
5. Close this disposition when the four Test defects and affected health are
   resolved and lawful B2 RED/GREEN progression is restored, or stop with one
   consolidated exception on a stated boundary. Normal already-authorized Gates
   need no new user relay. Final 91-AC, real GUI, retirement and independent
   Validator/acceptance obligations remain; do not claim completion early.

Controller scope/evidence review applies to this execution supplement; no new
product-plan readiness review or production Spec return is required. No new
installation, provider/real-data access, privilege, host/service/old WIP action,
cleanup, Git delivery, integration, archive or release permission. MacBook
publishes only this supplement and planning/status/event updates. Mini does not
write the project board. Old bootstrap remains BLOCKED / DISPATCH_ORPHAN_READY,
not CLOSED. Fixed publication and user forwarding are not receiver adoption.
