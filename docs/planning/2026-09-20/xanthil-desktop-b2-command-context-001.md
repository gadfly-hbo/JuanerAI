# B2 Command Context and Evidence Disposition 001

Date: 2026-09-20. Package: PKG-XANTHIL-DESKTOP-001.
Change: xanthil-desktop-membership-repurchase-decision-case.

## Approved decision and exact return point

The user approved one explicit Test evidence-directory channel, risk-based
evidence retention, and use of the one remaining B2 Test-content round. No new
Test allowance, product/UI decision, full Spec rewrite or production execution
on MacBook. This is a bounded execution-contract amendment to
[B2 Test Contract Disposition001](xanthil-desktop-b2-test-contract-disposition-001.md),
not an ordinary invocation typo repair or a product Gate PASS.

Controller attribution: the previous supplement required per-command emitted
output but froze every available context channel. Reviewing Test bodies without
closing capture -> child -> output-directory transmission missed that conflict.
Mini correctly stopped. The remedy below closes that channel, not product scope.

Continue only Mini task `纵切-1.0` / `01a0b45a-13ad-7b92-a2b4-cdde980558e1`,
repo `/Users/bendandebaba/JuanerAI`, branch
`work/macbook/whitepaper-blueprint-v1`, upstream
`origin/work/macbook/whitepaper-blueprint-v1`.
Stop HEAD `8078b9ab3d09ee8cf9507c5718a1e366be0fa84b`,
tree `bfee3bbd2603ebc79af789220ae5bb091eb39852`.
MacBook publishes on `work/macbook/wip-preflight-exception-disposition`; the
user forwards the fixed receipt to that original task. No automatic message,
new task or ownership change of Mini's branch.

The original Mini evidence directory, called **E** below, is:
`/Users/bendandebaba/JuanerAI-artifacts/xanthil-desktop-membership-repurchase-decision-case/mac-mini-wip-preflight-20260919-001/technical-decision-001`.
Latest `E/b2-test-contract-disposition-stop-index-001.json`: 108712 bytes,
SHA-256 `0eefa0539ce0784b269c1c705ee1cb46de1794763098fb7ee40ab2b0021f5cf5`.
Formal `E/b2-test-contract-conflict-return-001.md`: 6719 bytes,
SHA-256 `1e7d51c129024b515b1dea1a7438883a217094b49a4a06f980169081478678c8`.
MacBook matched current 34 sources/34 final copies and read the actual capture
and conflicting clauses. Its intake record is under the existing MacBook root:
`/Users/huangbo/JuanerAI-artifacts/PKG-XANTHIL-DESKTOP-001/dependency-structure-decision-20260919/incremental-contract-disposition-001/b2-command-context-001/controller-intake-001.json`.
This proves current byte identity, not missing historical evidence or product
behavior. Mini originals are not claimed to be backed up on MacBook.

## One explicit channel; no new runner

For the following existing command IDs only, the frozen child environment is
the original ten unchanged fields **plus JUANERAI_TEST_EVIDENCE_DIR**:

- H-B2-LOAD-U1.1
- R-B2-COMPONENT-U1.1 / G-B2-COMPONENT-U1.1
- R-B2-MOUNT-U1.1 / G-B2-MOUNT-U1.1
- REG-SAFETY-B2-MOUNT-U1.1 / REG-UI-B2-MOUNT-U1.1 / REG-FINAL-B2-MOUNT-U1.1
- REG-UI-B2-COMPONENT-U1.1 / REG-FINAL-B2-COMPONENT-U1.1

Each slash separates existing IDs. This list grants no execution before that
ID's existing lifecycle Gate. All other commands retain the original ten-field
environment and original capture. No global environment or product flag changes.

For each admitted invocation, **D** is its already approved, fresh absolute
directory `E/incremental-contract-execution-001/U1.1/<command-id>/<attempt>`.
Freeze `command.env.JUANERAI_TEST_EVIDENCE_DIR = D` in command.json before its
hash is fixed. Outer launch receives that same exact eleven-field environment;
capture continues passing `command.env` unchanged to the child. Preserve cwd,
absolute tool, timeout, argv, selector, selected counts and command/attempt IDs.
Use existing permitted attempt names and actual execution ordinals; never
overwrite the eight unused old envelopes or represent them as executed.

Preserve `E/u11-b1-capture-file-001.cjs` unchanged (4006 bytes; SHA-256
`7e2e4c8eb8265da3f358d0c135f9cedc577b2e8447b3baba1315ec86b948d60c`).
Formal Test may make one task-local derived version at
`E/b2-command-context-capture-001.cjs`, limited to:

1. Accepting only the context-bearing IDs listed above and the original ten
   environment keys plus the single new key; keep all original value checks.
2. Before output creation/child spawn, rejecting missing/empty/non-absolute or
   unequal context: the new value must equal the exact outer directory D,
   bound to the frozen command ID/attempt under the existing evidence root.
3. Updating the old ten-fields-only descriptive result text to record the
   actual frozen eleven-field environment. Keep full invocation/output/result
   capture, input readback, no-overwrite checks and macOS metadata handling.

The coordinator reviews the minimal delta, freezes its actual bytes/hash and
includes it as an input before use. No execution of an unfrozen capture; no new
schema, scheduler, registry, pointer, fallback, broad environment inheritance or
replacement runner. The original capture remains usable for unaffected commands.

The B2 driver consumes only this explicit environment field for its command
directory; it does not infer R/G/regression from argv or search for a latest
directory. Missing or invalid context is INVALID health before transpilation
or product import, never product RED. Generate fresh Test-owned .mjs and
source/emitted bytes/hash records beneath D, with non-overwrite writes. These
are outputs, not mutations of frozen command.json/inputs.json or inputs that
must remain absent in capture's post-readback. The exact Renderer source and
pinned TypeScript/React controls remain governed by the existing design.

This explicitly overrides only the ten-field/no-new-channel/no-capture-edit
clauses for these commands in test-plan.md, prior supplements and frozen role
briefs. The command-child requirement in path-contract.md is fulfilled, not
removed. The nine Spec bodies remain preserved; the coordinator records this
controlling execution amendment in the three stage records. No new formal Spec
dispatch or full Spec rewrite is required for this approved transport-only delta.

## Evidence retention and precise historical limitation

The user accepts only this latest role's disclosed missing native records for
manifest inspection, the invalid comparison using zsh `path`, tool location and
the corrected comparison, and the summary-only patch-result record. Their
locators are the formal return above and
`E/b2-test-contract-conflict-return-patch-result-001.json` (1559 bytes;
SHA-256 `597a858a82b59439d3137b614ebf71ebddeefe1d51088fa40e258137ad934310`).
Preserve both originals, failures and UNKNOWN. Summaries do not become raw
commands/API objects or a historical PASS. Root's current 34/201 readback does
not repair the role's lost history. Do not replay it or claim all effects absent.

Prospectively, for this batch, classify evidence by its use and actual effects:

- Ordinary authorized source browsing/text location needs useful source
  references, findings and accurate error disclosure, not archival of every
  API chunk_id/wall_time object. Missing such metadata alone does not stop work.
- Identity/hash admission checks, formal syntax/health/RED/GREEN/build/GUI
  verification, and operations with writes or external effects retain original
  evidence sufficient to prove the actual command/arguments, cwd/environment,
  relevant input identity, outputs/errors, numeric exit and completion state.
  Use the existing capture/native result, including necessary yielded-session
  completion. A summary or a combined command's last exit is not a substitute.
- A new unknown effect, completion/authority ambiguity or missing Gate-critical
  result still stops. Do not relabel identity verification as ordinary browsing,
  weaken assertions, or use this grading as a blanket retrospective waiver.

This narrowly supersedes the prior requirement to archive a complete native
API object for every ordinary read; no new logging framework or global policy
change is needed. Broader evidence requirements remain for consequential work.

## Role, budget, health and resumption

Preserve B2 content **1/2 used**, although that return made zero Test edits and
ran zero tests. After fixed intake, one fresh formal juaner_test / configured
model / R2 xhigh uses the remaining round **2/2** for this transport correction,
F1-F4 closure and affected verification. There is no free preparation/review
return or third content round. Invocation restoration keeps its existing
per-actual-cause limit and history; it is not authority to revise this contract.

Test retains only the prior five Test paths, plus the single derived capture
and necessary non-overwriting command/output/control artifacts in E. The Mini
coordinator owns verification.md, traceability.md and tasks.md; it does not
write Test or product code. All other received 34-file assets stay unchanged
during this repair, including B1 source, package/lock, CSVs, future tests and
the nine Spec bodies. Subsequent lawful Worker scope remains separate.

Before interpreting Renderer failures, verify the actual context handoff with
the existing narrow health mechanism: the admitted child observes its exact D,
and missing/mismatched directory values are rejected before transpilation or
product import. Any negative capture-control fixture stays in a fresh owned
evidence child, uses no product or provider and is recorded as a deliberate
health control, not an unexplained retry or product RED. These checks are part
of the same remaining Test round, not a new probe budget or runner project.
For executable admission, the positive case is the existing H-B2-LOAD command.
The two negative capture invocations are explicitly allowed control fixtures
under `E/b2-command-context-health-001/missing` and `.../mismatch`, outside the
normal command-attempt table solely to test rejection. They use the same derived
capture and frozen H-B2-LOAD argv, with the named missing/mismatched field; the
coordinator freezes exact outer invocation, local envelope/input identities and
expected pre-spawn refusal before use. Missing/mismatched context must be the
observed rejection, not an unrelated path/loader error. These control locations
never become product execution directories or substitutes for D.
Evaluate missing/value-equality guards before the regular directory-layout
check so these deliberate controls cannot pass for the wrong rejection reason;
do not relax that layout check for any real invocation.
Preserve known valid/invalid TSX controls, all F1-F4 assertions and affected B1
regression obligations. Merely printing the directory is not loader or product
proof. Failures remain INVALID health until resolved within the existing budget.

Before one fixed ff-only intake, verify receipt commit/tree/unique parent,
complete four-file diff/hashes, no overlap with current34, and those sources and
final copies; recheck afterwards. Keep branch/upstream and verify-and-skip an
already received commit. No reset/stash/clean/overwrite/early commit on Mini.
Then freeze the targeted capture/command/input changes and perform the one
remaining Test revision with its actual health -> causal RED -> scoped TDD_READY
return. Only the original independent Worker/GREEN/affected regression/first
package/GUI progression follows a valid Gate; no per-success user relay.

Close this supplement when explicit context transmission and F1-F4 are healthy
and B2 can lawfully progress, or return one consolidated exception for an
unresolved/exhausted, no-progress, unsafe or out-of-scope condition. No speculative
downstream implementation, extra Test/Worker/GUI allowance, dependency/provider/
real-data/host/old-WIP permission, cleanup, new Git delivery, merge or release.
B1 scoped GREEN, Stage0/1 and P1-P4 remain without replay unless an actual shared
input change requires its focused regression. Final91, real UI, retirement,
Validator and acceptance remain required. Old bootstrap is not CLOSED.
