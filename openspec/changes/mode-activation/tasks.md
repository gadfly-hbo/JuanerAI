# Tasks: Mode Activation

All tasks are serial under personal Global WIP `1`. The Spec result itself does not authorize any Test, Worker, Validator, host mutation, commit, push, or PR; each subsequent action requires authorization from the current Controller/user Gate.

## Gate 0 — Spec Freeze

- [x] `MA-TASK-001` Controller reviews the full OpenSpec package against the four frozen planning inputs, the latest explicit Controller/courier decision, and canonical Foundation.
- [x] `MA-TASK-002` Run mandatory `ponytail-review` on the complete OpenSpec diff because this R3 governance Change adds persistent trust, host process, recovery-sensitive installation, and protocol composition.
- [x] `MA-TASK-003` Freeze later repository paths, conditional host paths, fourteen canaries, external prerequisites, and Foundation-byte hashes; issue Spec Gate verdict.

## Gate 1 — Test Design and RED

- [x] `MA-TASK-004` Fresh `juaner_test` creates only the three allowed Test files/edits and the AC-to-TEST map.
- [x] `MA-TASK-005` Establish helper/environment health independently, then causal RED for trust/CLI/host/Agent/Git/GitHub/Ledger/revision/archive/release/rollback requirements.
- [x] `MA-TASK-006` Freeze Test hashes, commands, counts, lifecycle ledger, expected RED, and exact Worker repository paths; obtain TDD_READY.

## Gate 2 — Repository Implementation

- [x] `MA-TASK-007` Fresh `juaner_worker` implements the minimum governance policy/template/role/skill delta without duplicating the policy.
- [x] `MA-TASK-008` Implement Controller signer, production composition, root host loop, transport-only CLI, restricted adapters, installer/backup/rollback, and launchd template only in allowed paths.
- [x] `MA-TASK-009` Drive focused RED to GREEN, run affected integration and canonical offline regression, inventory exact paths, and prove Foundation core/spec bytes unchanged.

## Gate 3 — Test Asset Retirement and Candidate

- [x] `MA-TASK-010` Reconcile every Test asset as permanent regression, temporary evidence, or retirement candidate; run `ponytail-review` on the complete Test diff and close the Test Asset Retirement Gate.
- [ ] `MA-TASK-011` After Controller re-gate PASS, stage exact repository paths and create/read back a descendant replacement Candidate from `09a38c7d3656189241e6226b64a1f5f76f802c59`; only then mark `09a38c7d3656189241e6226b64a1f5f76f802c59` `SUPERSEDED`, preserve it as historical evidence, run final validation, and dispatch a fresh exact-Candidate read-only Validator.

## Gate 4 — External Prerequisites and Host Installation

- [ ] `MA-TASK-012` Controller obtains explicit administrator approval, separate repository deploy-key branch-push and read/read-write-minimized PR API credentials, protected-main no-bypass evidence, reliable permission metadata or deterministic no-merge-side-effect proof that the PR credential cannot merge, compatible Node/Codex runtime, and a dual-host Git `2.54.0` executable with equal SHA-256. Never call merge on a real product PR.
- [ ] `MA-TASK-013` Create the real MacBook Ed25519 key and install/read back Mac mini root-owned trust/config/two purpose-bound credentials/runtime/service using exact backup manifest. Prove cross-use is unavailable and never expose secret bytes.
- [ ] `MA-TASK-014` Prove sole host loop, non-root effective-write denial, best-effort Remote route/repository/fixed-CLI/exact-envelope integrity, SSH's install/rollback/emergency/explicit-backup-only boundary, sanitized logs, and restart fail-closed behavior.

## Gate 5 — Activation Canaries

- [ ] `MA-TASK-015` Execute every non-deferred obligation in `CAN-MA-01` through `CAN-MA-14` against the exact Candidate and installed configuration without a production-valid DISPATCH, direct production-adapter invocation, new Canary interface, or disposable WIP. Freeze complete sanitized evidence and preserve any FAIL. Record the prior real-signed admission-precondition rejection canary as retired/inconclusive and do not retry it; leave only the named `CAN-MA-07/08` production-positive effects pending for the first authorized real product Change.
- [ ] `MA-TASK-016` Reconstruct canonical diff on both devices, prove raw-byte hash equality, run secret/scope scans, and perform bounded rollback rehearsal with state/evidence preservation.
- [ ] `MA-TASK-017` Fresh Validator rechecks repository, host/trust, Remote route/envelope and missing-output fail-closed behavior, GitHub negatives and PR no-merge proof, dual-device, Test Asset Retirement, canary, backup, rollback, exact three-effect `CAN-MA-07/08` deferral, no-direct-adapter boundary, and stop-line evidence.

## Gate 6 — PR, Archive, and Integration

- [ ] `MA-TASK-018` Deliver the first Candidate through the current pre-Activation Git workflow; MacBook reviews fixed PR Head and issues signed archive REVISION only after review.
- [ ] `MA-TASK-019` Mac mini mechanically archives to `openspec/changes/archive/2026-08-26-mode-activation/`, publishes `openspec/specs/mode-activation/spec.md`, creates a descendant Candidate, reruns final validation/fresh Validator, and updates the same PR/Handoff.
- [ ] `MA-TASK-020` MacBook performs final review, squash merge, origin/main archive/canonical readback, local-main synchronization; Mac mini fast-forwards main only through the accepted bounded release procedure.
- [ ] `MA-TASK-021` Record `ACTIVATION_READY_AWAITING_FIRST_PRODUCT_CHANGE_AUTHORIZATION` and stop only after every non-deferred obligation and PR no-merge proof pass. Do not generate a product DISPATCH; record that the separately authorized first real product DISPATCH must supply the durable positive Remote/real-signature proof and the three deferred `CAN-MA-07/08` exact readbacks before any Acceptance.
