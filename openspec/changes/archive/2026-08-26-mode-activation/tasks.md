# Tasks: Mode Activation

All tasks are serial under personal Global WIP `1`. The Spec result itself does not authorize any Test, Worker, Validator, host mutation, commit, push, or PR; each subsequent action requires authorization from the current Controller/user Gate.

## Gate 0 — Spec Freeze

- [x] `MA-TASK-001` Controller reviews the full OpenSpec package against the four frozen planning inputs, the latest explicit Controller/courier decision, and canonical Foundation.
- [x] `MA-TASK-002` Run mandatory `ponytail-review` on the complete OpenSpec diff because this R3 governance Change adds persistent trust, host process, recovery-sensitive installation, and protocol composition.
- [x] `MA-TASK-003` Freeze later repository paths, conditional host paths, fourteen canaries, external prerequisites, and Foundation-byte hashes; issue Spec Gate verdict.

## Gate 1 — Test Design and RED

- [x] `MA-TASK-004` Fresh `juaner_test` creates only the three allowed Test files/edits and the AC-to-TEST map.
- [x] `MA-TASK-005` Establish helper/environment health independently, then causal RED for trust/CLI/host/Agent/Git/GitHub/Ledger/revision/archive/release/rollback requirements.
- [x] `MA-TASK-006` Freeze Test hashes, commands, counts, lifecycle ledger, expected RED, and exact Worker repository paths; obtain TDD_READY.

## Gate 2 — Repository Implementation

- [x] `MA-TASK-007` Fresh `juaner_worker` implements the minimum governance policy/template/role/skill delta without duplicating the policy.
- [x] `MA-TASK-008` Implement Controller signer, production composition, root host loop, transport-only CLI, restricted adapters, installer/backup/rollback, and launchd template only in allowed paths.
- [x] `MA-TASK-009` Drive focused RED to GREEN, run affected integration and canonical offline regression, inventory exact paths, and prove Foundation core/spec bytes unchanged.

## Gate 3 — Test Asset Retirement and Candidate

- [x] `MA-TASK-010` Reconcile every Test asset as permanent regression, temporary evidence, or retirement candidate; run `ponytail-review` on the complete Test diff and close the Test Asset Retirement Gate.
- [x] `MA-TASK-011` Preserve the completed Candidate lineage through exact local/remote Candidate `10ba759eefe3d4ff3bae08d5775882e28422fad8` and the fresh Validator `BLOCKED` verdict. Keep all earlier Candidates, validation, and Gate evidence historical; `10ba759e` is rejected evidence, not a final Candidate or PASS.

## Gate 4 — Bounded Safety Repair and Causal RED

- [x] `MA-TASK-012` After Controller re-gate PASS, fresh `juaner_test` changed only `mode-activation.test.mjs` and `git.integration.test.mjs` and established exactly two causal RED causes: Git UID `501` ACL/runtime-execution closure and exact production branch local-ref/Candidate/remote-predecessor binding. Final Test hashes are `9041985003453933c666caae7643fed9d283f6a529be7b9a5bdc5f33ab2a76b8` and `cd027e786e035ddeb505bfa87553365a2fbf91a8d80c3bb5398d2acda356e7c2`; historical rejected-Candidate evidence remains immutable.
- [x] `MA-TASK-014` The bounded Worker changed only `install-host-loop` to close Git installation authority without changing fixed executable/library bytes. Current SHA-256 is `c25e6634b9e225a893b83a027c2323606c84fb80e4049646d7d121fd02bed8ff`; the successful Host Install Gate proved complete backup/readback/rollback authority, search-only traversal, effective-write denial, and real UID `501` Git `2.54.0` with frozen SHA.
- [x] `MA-TASK-015` Preserve the closed Foundation repository-identity prerequisite as GREEN regression evidence: canonical spec `2633ad86...`, `coordinator.mjs` `4efc2f28...`, `coordinator.test.mjs` `de93ab87...`, fixtures `48b1265a...`, focused `180/180` PASS, canonical runner exit `0`, TAR PASS, archived history unchanged. The exact production contract is same-process verified `acceptedDispatch.body.repository.repository_id == 'gadfly-hbo/JuanerAI'` plus independent `config.github_repository == 'gadfly-hbo/JuanerAI'`; neither Foundation file is a remaining Test/Worker write path.
- [x] `MA-TASK-016` The bounded Worker changed only `production.mjs`, current SHA-256 `4fec4c2a5ad803a607fa799ff514dbe20669a5e7dd2c4f209bc190238d098509`, and uses the existing Git Gateway to read exact local `refs/heads/<branch> == Candidate`, require remote predecessor `== expected_remote_head`, push that exact Candidate to the same ref, and read back remote `== Candidate`; every mismatch stops before push or uses only the existing ambiguity boundary.

## Gate 5 — Production Safety GREEN and Non-deferred Evidence

- [x] `MA-TASK-017` GREEN is complete for exactly the two remaining RED causes. Full Coordinator regression passed `248/248` outside the sandbox after the sandbox-only Unix Socket `EPERM`; project-control passed `12/12`; the canonical runner exited `0`; syntax/plist/diff checks passed; Test Asset Retirement and `ponytail-review` found no temporary, duplicate, orphaned, skipped, ownerless, or speculative asset (`Lean already. Ship.`). Every non-deferred pre-Candidate `CAN-MA-01..14` obligation is frozen in `verification.md`. Only the three named `CAN-MA-07/08` product-positive effects remain deferred.

## Gate 6 — PR, Archive, and Integration

- [ ] `MA-TASK-018` Only after `MA-TASK-012`, `014`, `015`, `016`, and `017` complete, create/read back the next final implementation Candidate, run final validation and a fresh exact-Candidate Validator, and deliver it through the current pre-Activation Git workflow; MacBook reviews the fixed PR Head without issuing a Foundation REVISION or creating an active pointer.
- [ ] `MA-TASK-013` Only after that first PR review, MacBook Controller mechanically archives to `openspec/changes/archive/2026-08-26-mode-activation/`, publishes/readbacks `openspec/specs/mode-activation/spec.md`, and creates a descendant Candidate on the same branch/PR. This is a Controller repository mechanic, not Worker production implementation or causal RED. Static contract/path tests remain regression evidence. The exception never applies to a product Change; no Mac mini archive, DISPATCH, active pointer, or Foundation REVISION is used.
- [ ] `MA-TASK-019` Run final validation and a fresh Validator against the Controller-created archive descendant, then update the same PR/Handoff with exact archive/canonical/Candidate readback evidence.
- [ ] `MA-TASK-020` MacBook performs final review, squash merge, origin/main archive/canonical readback, local-main synchronization; Mac mini fast-forwards main only through the accepted bounded release procedure.
- [ ] `MA-TASK-021` Record `ACTIVATION_READY_AWAITING_FIRST_PRODUCT_CHANGE_AUTHORIZATION` and stop only after every non-deferred obligation and PR no-merge proof pass. Do not generate a product DISPATCH; record that the separately authorized first real product DISPATCH must supply the durable positive Remote/real-signature proof and the three deferred `CAN-MA-07/08` exact readbacks before any Acceptance.
