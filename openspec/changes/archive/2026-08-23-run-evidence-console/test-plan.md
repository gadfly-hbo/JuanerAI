# Test Plan: Xanthil Run & Evidence Console

## Status and Test Isolation

This is Test Design intent only. The Test role writes all tests after Spec Gate; the Worker does not edit them. Causal RED must occur against healthy native TypeScript/no-emit tooling and absent reader behavior. The real-model gate is inapplicable: no model/provider invocation is allowed.

## Planned Evidence

| TEST ID | Level | ACs | Essential proof |
|---|---|---|---|
| TEST-REC-001 | unit | 002-02, 002-03, 003-03 | unchanged existing Run Manifest/Evidence Index validators plus reader-owned full nested persisted confirmed-contract admission; independently mutate non-`1.0` `schema_version` in `run.json`, `analysis-contract.json`, and `evidence.json` to exactly `RUN_CONTRACT_UNSUPPORTED`; separately prove fatal UTF-8/duplicate-member and unknown/missing/null/type/enum/format/cross-reference failures, including empty Finding statement/limitations/limitation text, are `RUN_READ_FAILED`; exact runtime result keys plus TypeScript result-envelope and neutral Artifact-1.0 provenance shape |
| TEST-REC-002 | unit | 003-01..03 | succeeded/non-success projection separation and abandoned-candidate label |
| TEST-REC-003 | unit | 004-01..03 | core-SHA versus indexed-size+SHA semantics, shape-valid Evidence relationship/pointer resolution, Markdown non-authority, strict UTF-8/media-type display admission |
| TEST-REC-004 | contract | 001-01..03, 002-01, 005-01..03, 006-01..03 | unchanged one-operation reader Port driver against deterministic double and local Adapter, including propagation of reader `RUN_CHECKSUM_MISMATCH`; phase-one manifest-only observation, admitted business/standard observations, and Port-independent Core |
| TEST-REC-005 | integration | 002-01..03, 004-01, 005-01 | real two-phase containment/stability and declared-asset-byte-size behavior, including pre-read indexed-size mismatch as `RUN_CHECKSUM_MISMATCH` through the real Profile; atime/read probe proving unaccepted manifest opens no second file; nonexistent/unreadable declared source path; controlled-workspace no-write snapshots |
| TEST-REC-006 | integration | 003-01..03, 004-01..03 | valid exact successful Run resolves full requested evidence/provenance view |
| TEST-REC-007 | integration | 003-02, 005-03 | failed/cancelled/in-progress only yield permitted non-success or exact rejection |
| TEST-REC-008 | negative | 002-01..03, 004-02, 005-01..03 | malformed selection; unknown Evidence field/invalid Finding status are `RUN_READ_FAILED`, while only shape-valid dangling/foreign refs are `RUN_REFERENCE_INVALID`; static Adapter/Experience filesystem-call/import closure excludes mutation and source-path access; Port/Profile/import closure excludes other forbidden capabilities |
| TEST-REC-009 | E2E | 001-01..04, 004-03, 005-04, 007-01..02 | exact `node … --run <absolute-run-directory>` one-Run launch; loopback-only assigned URL; labelled `<pre>` display text for SQL/Python/JSON/Markdown; hostile persisted content is strict-UTF-8 escaped inertly with no Buffer-array, injected element/script/navigation/execution; 404 other route/method; no browser/open/switch API; Ctrl+C close |
| TEST-REC-010 | regression/scope | 006-01..03, 007-03..05 | AC coverage map and test-asset lifecycle ledger; no writer imports/changes or forbidden surface/effect; typecheck and canonical offline validation. The exact 35-file TypeScript graph remains solely TEST-XCLI-021 / REC-CONTRACT-003 evidence. |

## Required Mutations and Negative Cases

Independently schedule missing/relative/duplicate/unknown CLI argument; selected-directory symlink; manifest path escape/non-regular/stability failure; atime/read probe proving malformed manifest causes no other file open; non-`1.0` `schema_version` independently in each named `run.json`, `analysis-contract.json`, and `evidence.json` as exactly `RUN_CONTRACT_UNSUPPORTED`; fatal UTF-8 and duplicate object member at every document depth; confirmed-contract nested unknown/missing/null/type/enum/format/cross-reference failures across time/questions/sources/windows/metrics/signal/output/constraints; named-machine-document basename/run-ID mismatch; each terminal-status misuse; missing success document/asset; core-document SHA mismatch; indexed-asset declared byte-size/hash mismatch; unknown Evidence field and invalid Finding status as `RUN_READ_FAILED`; only shape-valid bad/foreign/dangling/duplicate Finding, Evidence, source, asset and JSON Pointer relationship as `RUN_REFERENCE_INVALID`; unsupported media/malformed textual asset; hostile HTML text and asset bytes; partial candidate; I/O permission/read failure; non-GET/root request; and interrupt shutdown. Each asserts the applicable minimal reader result/code. TEST-REC-005 alone snapshots the complete controlled temporary workspace before/after a success read, including outside the selected Run, with a nonexistent/unreadable declared source path; zero mutation and zero source dereference are required. TEST-REC-008 alone proves static Adapter/Experience closure for no mutation/no source-path access. Port/Profile/import closure proves absent model, network, credential, project-control, writer, and action capability.

For REC-CONTRACT-004, Test retains exact runtime-key assertions and TypeScript proof for the reader-result envelope and provenance. It removes or defers compile-time-only guards that require complete TypeScript closure of nested Artifact-derived View values or status-specific detail. This does not narrow any machine-document admission mutation or runtime output assertion.

## Test Asset Lifecycle Ledger Expectation

At TDD_READY the Test role records every new test, fixture, helper, double, snapshot, and coverage-map leaf. Causal RED, Port contract driver/double, malformed/tampered fixture, and E2E host fixture are presumed permanent regressions only if they map to a retained TEST/AC and have a current consumer. Temporary diagnostic probes remain in `/private/tmp`. The Controller must reconcile the complete test-asset diff, run `ponytail-review`, and record a Test Asset Retirement PASS before Validator dispatch.

## Commands and Evidence Level

The Test role must freeze exact changed-file commands at RED, including targeted native `node --test` files and static import/forbidden-path plus before/after filesystem checks. At the authorized Test/implementation step, existing TEST-XCLI-021 / REC-CONTRACT-003 evidence exclusively verifies that `tsconfig.json` preserves every existing entry/option and appends precisely the accepted 14 paths; then `npm run typecheck` and `tools/harness/validation/run` include Console paths. TEST-REC-010 retains the coverage map, lifecycle ledger, forbidden-scope/import, typecheck, and canonical evidence only. No package modification/install, model invocation, source/network access, or browser automation is permitted. E2E uses Node standard-library HTTP against the printed loopback URL.
