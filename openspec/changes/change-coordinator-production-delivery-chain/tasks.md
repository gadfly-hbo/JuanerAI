# Tasks: Change Coordinator Production Delivery Chain

<!-- R216_SPEC_BEGIN -->
## R216 — finite F1/F2 correction task application

R216 extends only existing tasks; it creates or completes no task by this Spec return.

| Gate / existing task | R216-bounded obligation |
|---|---|
| `TASK-M2-001..003` | Controller reviews the six-marker addendum against R214, Foundation Ledger publication, R213 F1/F2 protection, unchanged proposal and current source; runs mandatory complete-diff `ponytail-review`; records a new Spec Gate decision. High-reasoning Spec output itself does not pass the Gate. |
| `TASK-M2-010` | Fresh isolated Test freezes only `production-delivery-chain.test.mjs` F1 `R216-L01..L05`—verified unused before this Spec—and one bounded `coordinator.test.mjs` F2 group, exact preimages/counts/selectors/timeouts/oracles/suffixes and permanent disposition. It separates eleven-field STATUS from sixteen-field LocalPause and rebinds six crash-window fault/request observations without preserving the old short request or exact-two-call coupling. All other Test paths are frozen absent a concrete contract return. |
| `TASK-M2-011` | With production frozen, prove `RED-R216-F1-ENTRY` only at the missing named export and freeze F1 suffixes as `NOT_REACHED`; independently freeze/run the existing-entry F2 causal REDs without granting them implementation authority. |
| `TASK-M2-012` — checkpoint `TDD_READY-ENTRY` | Controller verifies entry causality and healthy module/Git/filesystem/oracle controls, then may authorize only mechanical extraction/delegation in `production.mjs`. No F1 validation/error fix and no `coordinator.mjs` change is allowed. |
| `TASK-M2-033` — entry-only Worker pass | Export `createUnpublishedLedgerEvidenceCommitBuilder`, move the existing local work/index/hash/update/tree/commit body into its `construct`, expose only the temporary internal `{tree,commit}` result, and make the private writer delegate only while `prepared.publication` is null. Preserve old deficient result handling/omitted preservation and original successful-publication reuse. Do not touch protected transport, receipt success, credentials, Core, or another production file. Return exact source/preimage comparison and stop; the following isolated Test checkpoint, not Worker, owns independent healthy mechanical-publication readback. |
| `TASK-M2-011` — behavior checkpoint | After Controller source-preservation acceptance and independent mechanical-publication health, run unchanged `R216-L01..L05`; require genuine derived-index-lock child failure plus independently read physical bad-publication facts as `RED-R216-F1-BEHAVIOR`, never return shape alone. Record controlled inputs, actual fault executions and non-forceable result branches separately, and retain the separately causal F2 REDs. |
| `TASK-M2-012` — final TDD_READY | Only after the behavior RED and complete frozen suffix review may Controller authorize the minimum F1 final correction in `production.mjs` and F2 correction in `coordinator.mjs`. Exact write set is those two production paths; a need for any Adapter, Host, fixture, shared helper, schema, Port signature, State writer, or third production path blocks for contract review. |
| `TASK-M2-033..034` — final Worker pass | In the already extracted body implement the fourteen-field snapshot, prior prefix/physical old-blob/canonical-tail binding, exact Git outputs/headers/error priority including recognized wrong `tag` as conflict, object/byte/preserved-entry proof, and private publication rules: non-OK leaves null/current-call no push/later caller may re-enter; one OK is then reused; no builder retry. In `coordinator.mjs` correct only eleven-field STATUS/read-only parsing and READY admission use of existing strict Ledger signatures: `INVALID` requires persisted LocalPause; without it return existing `CoordinatorErrorV1 / status / REJECTED / WIP_AUTHORITY_INVALID`. Preserve all R216 exclusions and six crash-window meanings without old oracle/call-count coupling. |
| `TASK-M2-040..043` | Run frozen focused F1/F2 GREEN and fixed R213 regression sequence, complete M2 once, scope/canonical checks and Test Asset Retirement. Retain permanent F1/F2 assertions and every formal pre/post inventory, readback, exact source/Test identity, complete inner/outer log, session, exit and freeze reference; remove only confirmed non-formal Test scratch resources, with no new cleanup Gate. |
| `TASK-M2-044..045` | Freeze the repaired source/Test/evidence identity for one fresh read-only Validator. Only Controller acceptance can close the R212 branch and return to M3/M4; R216 does not dispatch or claim that result. |

The entry-only Worker must stop before behavior execution. The final Worker cannot use the entry RED as authorization for a fix, and cannot edit Test. Neither Worker may expose the full private Ledger factory, credentials, transport, arbitrary Git controls, a legacy/test mode, callback seam, persistent state, lock, retry, recovery boundary, or cleanup transaction. Local failed construction may leave bounded work/index/unreferenced objects but no ref/remote call. F2 changes no interface or producer signature and must preserve State writes/CAS, pause-first-write, normal non-READY run, settlement, admission/reauthentication, Candidate/Validator budget, PR/Handoff and Host behavior.

No checkbox below is changed. Status is `SPEC_READY / CONTROLLER_GATE_PENDING_R217`; F1/F2 and B1–B5 remain open, F3 withdrawn, and A2/A3 unchanged. Spec Gate, Test Design, each RED, each TDD_READY, both Worker passes, GREEN, regression, Retirement, fresh Validator, acceptance and lifecycle advance remain distinct Controller decisions.
<!-- R216_SPEC_END -->

<!-- R174_STATE_FIRST_WRITE_BEGIN -->
## R174 — bounded State seam task application

R174 applies existing tasks without creating or renumbering a task:

| Gate / existing task | R174-bounded obligation |
|---|---|
| `TASK-M2-001..003` | Review this six-file clarification, run mandatory complete-diff `ponytail-review` because the Spec route is high, and record Controller Spec Gate; proposal and all prior contracts remain unchanged. |
| `TASK-M2-010` | Fresh isolated Test freezes only the necessary import/local block in `coordinator.test.mjs`, exact source/Test identities, selectors/counts/timeouts, independent export and temporary-filesystem health controls, all seven suffix groups, permanent-retain disposition, and the two Worker source boundaries. The #56 connection keeps public result State fields null while preserving the existing-State-double READY bytes/version/hash and their diagnostic binding; only pause methods use real filesystem. |
| `TASK-M2-011` | With production frozen, run only the missing-export reachability RED; suffixes remain `NOT_REACHED`. After export-only source acceptance and healthy temp FS, run the separate real first-write RED: expected `OK`, actual `ABSENT`, no file. Do not count either as `TEST-M2-011 / 011-L08,L09`. |
| `TASK-M2-012` | Controller first releases only export-only TDD_READY; after exact source restoration proof and causal first-write RED, separately releases only the pause-private first-create TDD_READY. |
| `TASK-M2-034` | In this corrective slice Worker owns only `production.mjs`: first add `export` to the existing factory with its body unchanged; later change only `writeLocalPause` first-create-private logic. Shared `casWrite`/read/`atomicWrite`, pointer/State methods, all other production files, and Core remain frozen. This does not reopen the original run/`TEST-M2-011` contract. |
| `TASK-M2-040..043` | Run the frozen seven groups and original #56, then the fixed affected regression sequence; reconcile the new block as permanent/no retirement and prove only the two approved implementation regions changed. |
| `TASK-M2-044..045` | Freeze exact implementation/evidence for a fresh independent Validator; only Controller acceptance returns the original M2 line toward M3/M4. |

No box is checked by this Spec return. Spec Gate, Test Design, RED, either TDD_READY, implementation, GREEN, regression, Retirement, Validator, acceptance, external action, and lifecycle advance remain separate. Any need for another Test/production path, new Core export, composition input, State/error/schema/lifecycle behavior, lock/retry/recovery/durability machinery, real Host/credential/network action, or changed exact test mapping returns `BLOCKED / CONTRACT_CHANGE_REQUIRED` to Controller.
<!-- R174_STATE_FIRST_WRITE_END -->

> Current R109 Specification return (2026-09-09): the user-approved R107 C2 object-reader contract is synchronized with the original S21/R085 and unaffected R047/R059/R063 contracts. [Verification](verification.md) and [MASTER_PLAN A2](../../../docs/planning/2026-09-05/automation-repair/MASTER_PLAN.md#用户风险豁免-a2s09-环境验证不再阻塞2026-09-06) remain authoritative. Production/Test are frozen and S09 stays USER_WAIVED / NOT_VERIFIED. The current return point is the Controller's complete correctness/ponytail Gate, not Test Design, production, or VM preparation.

## Status and Ordering

- Current status: `SPEC_READY / CONTROLLER_GATE_PENDING_R109`
- Change: `change-coordinator-production-delivery-chain`
- Sequence: Spec Gate -> Test Design -> causal RED -> TDD_READY -> one minimum production implementation -> K1 -> K2 -> Regression/Retirement -> independent verification -> Controller acceptance
- Production order inside M2: B1 -> B2 -> B3 -> B4 -> B5
- Internal checkpoints: K1 closes only Regression-to-Candidate behavior; K2 starts from that proven Candidate and closes Candidate-to-Handoff behavior.

Unchecked boxes are planned work, not evidence that a Gate or behavior has passed. No task below authorizes its own successor.

## 1. Specification Gates

- [ ] `TASK-M2-001` — Controller reviews all seven Change files for correctness against C2-C7, B1-B5, the Foundation, PSP, and WVEB baselines.
  - Confirm the new Candidate branch is the only limitation on the old unconditional Candidate rejection; the complete WORKTREE behavior and WVEB Test body remain unchanged.
  - Confirm failure evidence, durable BLOCKED, and local pause are not conflated across any file.
  - Confirm no exact field, enum, nullability, hash, identity, recovery, or stop decision remains for Test or Worker to invent.
  - Confirm Design 5.2 and the Test command/oracle references use exactly `git diff --cached --raw -z --full-index --no-abbrev --no-renames <head_sha> --`, preserve full-40/zero-OID/exact-equality rejection, and add no fallback/config/parser/API/State behavior.
  - Confirm CCR004 changes only the existing four-key `readCommit` success parent to allow null after physical commit bytes prove zero parent headers; nonroot reads remain full 40-hex, failures never become root success, and Candidate creation/success/event/State/convergence/readback remains exact one-parent/non-null/full-40.
  - Confirm CCR003 changes only the existing Host launcher's trusted result-root preparation reachability and exports the existing Handoff factory, while production fixed roots/trust/configuration, shared process/file logic, public contracts, A2, and all original R044 finite gaps remain unchanged.
  - Confirm S21 changes only original009/010: accepted raw stdout remains an exact Buffer, path stdout uses the same minimum byte mechanism, both streams have exact length/full hash, the complete result uses the explicit fresh byte-normalized receipt preimage, Core independently verifies/derives paths, and exactly two cumulative observations remain. Confirm restart uses only existing persisted `canonical_diff_sha256` and unchanged delivery-ID preimage plus fixed immutable producer/object reconstruction, without claiming a persisted old path receipt or adding Candidate/Delivery fields; retain `009-L10` evidence-conflict versus actual PR/Handoff ambiguity mapping.
  - Confirm C2 exports only `createLedgerObjectReader({repositoryRoot,gitExecutable,runtime_uid,runtime_gid}) ->` frozen `{read}`, keeps the full Ledger factory/protected transport private, and maps its exact request/result/commands/errors and real commit-A/tree-B refusal only to `AC-M2-005-03`, original `010-L13`, and `TASK-M2-033`.
- [ ] `TASK-M2-002` — Controller runs the mandatory complete-diff `ponytail-review` and returns every material deletion/simplification finding to Spec.
  - Pass condition: the package adds no second orchestrator, registry, service, persistent transaction, new lock, fifth recovery boundary, event class, macro state, generic artifact gateway, or compatibility bag.
- [ ] `TASK-M2-003` — Controller records Spec Gate PASS or returns one bounded revision brief.
  - Spec Gate alone authorizes only the fresh Test role. It does not authorize production changes or claim K1/K2.
  - The old pre-CCR003 Spec Gate PASS is historical input and does not approve this delta.
- [x] `TASK-M2-004` — User explicitly approved `DECISION-M2-REPAIR-EVIDENCE-001`; Design 7.3 records the bounded derived-input contract, while 7.4-7.5 preserve its immutable-base authority alongside the separately approved CCR002 delta.
  - The signed Test base bytes/hash remain immutable. Only eligible attempt-zero Validator remote evidence may produce the repair-only nested binding and fixed effective input.
  - This completed decision is Specification input only. It does not pass the Spec Gate or authorize Test Design/production work.
- [x] `TASK-M2-005` — User approved `DECISION-M2-REPAIR-RUNTIME-CONTENT-002`; Design 7.4 freezes its one purpose-bound Host producer, sealed-content execution, closed proof artifact, isolation/preflight, cleanup, budget, publication, and negative contract for this same Spec Gate.
  - Approved scope: one purpose-bound, sandbox-preserving Host producer of trusted final-Test-byte and Candidate-tree evidence for every mapped RED/control execution; no implementation authority follows.
  - CCR002 adds exactly the repair Test RESULT `repair_proof`, repair Worker binding `repair_proof`, and repair Worker RESULT `repair_delivery` branches needed to carry Host proof/Test identity into the existing AGENT_RUN, Regression, and STAGE chain. These fields are not attributed to the earlier input-only approval.
  - Current Gate dependency is fresh Controller correctness/ponytail review of the frozen producer, executable, sandbox, closed schema, lifecycle, paths, consumers, and A-to-B/production-modify-restore negatives; the decision is no longer pending.
  - JSONL command events, artifact/TAP declarations, and post-close hashes cannot satisfy this task.

## 2. Test Design and Expected RED

- [ ] `TASK-M2-010` — A fresh `juaner_test` freezes the exact Test write set, pre-change file hashes and Test counts, stable Test IDs, commands, runtime, and helper-health evidence.
  - Candidate Test paths are only those listed in `proposal.md` and `test-plan.md`.
  - The accepted WVEB Test body is byte-identical.
  - Repair execution-content Test/assets remain unauthorized until Spec Gate; the Test base remains one immutable content-addressed byte string/hash, and later Test Design must derive the exact closed plan/proof schemas from Design 7.4.
  - `production-delivery-chain.test.mjs` is created only if a continuous public-chain Test cannot remain visible in an existing focused integration file.
  - Freeze the shared-launcher and Handoff factory reachability leaves separately from their full suffix assertions; keep every original R044 F01/F02/F04/F05 asset and causal-frontier item in the same finite map.
  - Freeze the independent `TEST-M2-012/012-L06` command and resource boundary as the only true one-hour offline run. It uses a real fixture child and current-user temporary root, never a shortened timer or clock hook.
  - Apply A2 only to its mapped privileged fixed-profile/sealed-proof and dependent repair K2 leaves as `USER_WAIVED / NOT_VERIFIED`; do not restore a VM/environment preflight, install, or fallback.
  - For S21, change only directly affected original009 assertions/minimum helpers in `tools/harness/change-coordinator/git.integration.test.mjs` and directly affected original009/010 registrations/minimum helpers in `tools/harness/change-coordinator/production-delivery-chain.test.mjs`; no other Test path is authorized. Retain accepted raw-Buffer/hash assertions and add path-Buffer/length/full-hash/explicit-receipt/Core-parse/restart evidence. Freeze every suffix before TDD_READY, but count only the actual missing interface/behavior as causal RED; report a nonzero path-hash mutation `NOT_REACHED` unless a real healthy prefix reaches it. Preserve IDs, unaffected assertions, `009-L10`, and `010-L01`; no compatibility duplicate or parser seam is retained.
  - R109 reuses original `TEST-M2-010 / 010-L01,L05,L13` and `TEST-M2-011 / 011-L08,L09`; no new Test ID or A2 skip is allowed. C1 freezes fresh-Core reauthentication positive/negative zero-effect assertions, including existing-local-pause refusal. C3 freezes public-run request/event-tip/hash/original-State diagnostic bindings plus local-pause persistence failure. C2 keeps the Core-double consumer matrix separate and freezes the real shared-reader commit-A/tree-B Adapter refusal, including its independently observed permitted manual diagnostic.
- [ ] `TASK-M2-011` — Test proves the six independent causal RED frontiers `RED-M2-001..006` with all production files frozen.
  - RED must fail at the missing approved behavior, while unrelated Foundation/WVEB/Git/host controls remain healthy.
  - Real temporary files, Git repositories/worktrees/bare remote, stage/commit/readback, validation children, and public Coordinator methods are required where named.
  - For CCR003, only absence of `createHostAgentLauncher` and absence of the exported `createHandoffGateway` are causal interface RED. Their prewritten dependent assertions are `NOT_REACHED`, not extra RED/PASS. Factory-independent fixture/process/filesystem oracles must pass.
- [ ] `TASK-M2-012` — Controller reviews RED causality, requirement coverage, no-side-effect assertions, Test Asset Readiness, and the exact four-file Worker boundary.
  - Pass condition: each `TEST-M2-001..012` owner has positive, negative/boundary, failure, and forbidden-effect coverage or an explicit not-applicable explanation.
  - Only a recorded `TDD_READY` unlocks production work.
  - Host proof remains mechanical evidence only and cannot replace the Controller's later RED causality decision.
  - TDD_READY requires the complete frozen suffix assets and healthy independent controls before production reachability is added. No post-Worker Test authoring may be deferred; after the new entries exist, those same assertions must execute before GREEN.
  - A2-mapped privileged evidence remains waived/not verified and is not a current TDD_READY prerequisite; ordinary Host/Handoff and all other runnable evidence remain mandatory.

## 3. Minimum Production Change — B1 then B2 (K1)

- [ ] `TASK-M2-020` — Implement B1 definition admission and evidence closure in `coordinator.mjs` and `production.mjs`.
  - Admit exactly the three signed six-field definitions and derive the fixed ordered eight-field definitions.
  - Build one fresh post-Worker `WorktreeSubjectV1`; execute affected Regression then Test Asset Retirement; validate both twenty-four-field receipts against the same Head/scope/snapshot.
  - Persist and remotely read back their complete `VALIDATION_RESULT` records before STAGE.
  - Covers `REQ-M2-001`, the B1 portion of `REQ-M2-005`, `TEST-M2-001`, and `TEST-M2-002`.
- [ ] `TASK-M2-021` — Implement B2 lossless dirty-tree inspection and exact stage in `production.mjs` and `adapters.mjs`, with Coordinator consumption in `coordinator.mjs`.
  - Observe the complete actual NUL-delimited path/status/content set, require empty index and legal dirty scope, and never use scope rules as stage arguments.
  - Expose only the purpose-bound `stageExact/readStaged` factory described by Design; keep `production.mjs` the sole production WVEB evaluator consumer.
  - In both cached-raw reads use the exact Design command with `--full-index --no-abbrev --no-renames`; require full 40-hex old/new OIDs, full zero OIDs, and exact blob identity without prefix acceptance or fallback.
  - Fence the second Regression snapshot through literal stage and independent index/filesystem content equality; require no remainder and a changed index tree.
  - Covers `AC-M2-002-01..05` and `TEST-M2-003`.
- [ ] `TASK-M2-022` — Complete Candidate commit/readback and K1 publication in `coordinator.mjs`, `production.mjs`, and `adapters.mjs`.
  - Implement only CCR004's bounded `readCommit` root representation from successful physical commit bytes at the existing `canonical_root` cwd, retaining the same request/four success keys/Gateway receipts and existing nonroot meaning; no failed parent lookup, empty output, graph observation, all-zero/self-parent, or uncertainty may synthesize `parent:null` or another success.
  - Use the deterministic per-Candidate idempotency identity, explicit `commit-tree <verified-tree>`, exact message trailer, and old-parent `update-ref` CAS; prove parent/tree/message/branch/Head/common-dir/empty-index/clean readback.
  - Keep `expected_parent`, `commitCandidate` success, physical Candidate parent count, `CANDIDATE_COMMITTED`, Candidate State, convergence, and Candidate readback exactly one-parent with the accurate non-null, nonzero, non-self full 40-hex parent; reject null, zero, self, missing, multiple, or wrong Candidate parents.
  - Append/read the complete `CANDIDATE_COMMITTED` event, then atomically persist/read Candidate State at `DELIVERING / FINAL_VALIDATION` with empty Candidate-only refs.
  - Exercise only the existing Candidate-commit recovery boundary on uncertain output.
  - Run this commit/readback step inside the same public `run` and mutex hold as TASK-M2-021; publish no intermediate CANDIDATE_COMMIT State.
  - Covers `AC-M2-002-06..07` and `TEST-M2-004`.
- [ ] `TASK-M2-023` — Controller reviews K1 evidence before any K2 completion claim.
  - Required evidence: public four-method path to the real Candidate, both Regression receipts and Ledger bytes, exact staged path/content/tree chain, Candidate event and State readbacks, negative frontiers, and zero later effects on failure.
  - K1 does not authorize or imply Candidate Final Validation, Validator, publication, PR, or Handoff PASS.

## 4. Minimum Production Change — B3 through B5 (K2)

- [ ] `TASK-M2-030` — Implement B3 closed Candidate Final Validation in `production.mjs` with Coordinator integration in `coordinator.mjs`.
  - Retain the entire WORKTREE branch; add only `final-validation-candidate / CANDIDATE` with the exact subject and twenty-four-field branch table.
  - Run one real child with WVEB lifecycle/output/timeout rules and real pre/post Candidate Git observations.
  - Remotely read the PASS receipt before adding Final Validation to Candidate-only refs.
  - Covers `REQ-M2-003` and `TEST-M2-005`.
- [ ] `TASK-M2-031` — Implement B4 Validator artifact courier and Coordinator authority in `host-loop.mjs` and `coordinator.mjs`.
  - Host reads, schema-checks, hashes, and forwards the exact canonical artifact; it does not synthesize verdict, Head, classifications, or empty notes.
  - Coordinator binds settlement/artifact/current Candidate and derives the exact twelve-field Validator receipt.
  - In `host-loop.mjs`, export the one closed `createHostAgentLauncher({resultRoot,prepareResultRoot})`; move the existing launch body into that shared implementation without adding a second path. Production main supplies fixed `AGENT_RESULT_ROOT` and the unchanged mkdir/chown/chmod callback; Test supplies only a real temporary root/local prepare operation. Preserve exact artifact input, argv/cwd/env/stdin, child/error/close/parser/inventory/re-read/settlement logic and Design 6.4's error order.
  - Covers `AC-M2-004-01..03` and `TEST-M2-006`.
- [ ] `TASK-M2-032` — Implement the approved one causal repair and all prohibited Validator continuations in `coordinator.mjs` and `host-loop.mjs`.
  - Blocked until a subsequent Spec Gate PASS, Test Design/causal RED, and TDD_READY; no partial repair implementation is authorized. A2 leaves the mapped privileged proof execution `USER_WAIVED / NOT_VERIFIED` and adds no environment prerequisite.
  - Host Loop alone reads the Agent plan, materializes/seals Candidate plus final Test bytes, performs the fixed-profile per-finding RED/control proof, cleans temporary roots, and publishes the distinct Host-owned result artifact; Coordinator verifies its complete binding before Worker.
  - Reuse the configured root-owned `node_executable`, `git_executable`, runtime uid/gid, existing one-hour deadline, output limits, RESULT/Ledger/Handoff path, and four production files. Do not add a Runtime/Gateway/event/State/recovery mechanism or fallback sandbox.
  - Read back the failed Validator RESULT/receipt pair, derive and round-trip the complete canonical evidence bytes, keep signed Test base bytes/hash unchanged, and atomically/readably consume attempt `0 -> 1` before REQUESTED.
  - Carry the exact repair-Test evidence and repair-Worker proof binding branches through Pending/Action/Settlement/AGENT_RUN and role-receipt hashing; deterministically converge a restart at the existing Ledger boundary and reject every late or substituted binding.
  - JSONL/post-close observations remain rejected as causal proof. Only Design 7.4's new fixed-content Host execution may produce repair PASS; budget/order and every proof failure follow Design 7.5.
  - Only after repair Test RESULT/receipt readback bind Worker, both Regressions, Retirement, new Candidate, Final Validation, and Validator to attempt 1 and the new Head.
  - Pre-consumption eligibility/source failures use no budget. After `0 -> 1`, wrong bytes/hash/schema/mapping/execution/profile/snapshot/restart/late outcomes keep the budget consumed, request no Worker continuation or proof retry, and every attempt-one Validator FAIL remains terminal without reset.
  - Covers `AC-M2-004-04..07`, `TEST-M2-007`, and `TEST-M2-008`.
- [ ] `TASK-M2-033` — Implement B5 canonical path evidence, publication identity, PR, Handoff, and State completion across `coordinator.mjs`, `production.mjs`, and `adapters.mjs`.
  - Preserve the exact raw `JUANERAI_GIT_DIFF_V1` Buffer; return NUL name-status stdout through the same Buffer mechanism with exact lengths/full hashes; hash the complete seven-field semantics through the explicit fresh byte-normalized receipt preimage; have Core independently copy/recompute/parse A/M/D/T records/derive and compare the sorted unique paths; and distinguish Candidate increment paths from cumulative Handoff paths.
  - Keep exactly the Freeze and Handoff cumulative canonicalDiff calls. Freeze stores only existing raw hash and delivery identity; Handoff/restart reconstructs from the same immutable objects/fixed producer and matches stored raw hash plus the unchanged delivery preimage/ID. A valid-but-different second diff remains `EVIDENCE_CONFLICT` with no Handoff call; actual PR/Handoff ambiguity retains `FINAL_HANDOFF_PR_AMBIGUOUS`. Do not add a Candidate/Delivery field or claim old path bytes were persisted.
  - Verify branch push/readback and freeze, derive the stable delivery/idempotency ID before PR, create-or-reuse/read the PR, and read canonical remote Ledger bytes.
  - Select actual per-attempt Test/Worker/Regression/Retirement/Final/Validator evidence; construct/read exact Handoff without future HANDOFF_READY self-reference; then append/read HANDOFF_READY and persist/read `AWAITING_CONTROLLER`.
  - Export only the existing `createHandoffGateway(stateRoot)` from `production.mjs`; retain the same `atomicWrite` implementation and keep complete Handoff/Ledger/evidence validation in Core. At the gateway boundary require non-empty canonical JSON bytes, safe Change ID, full hash, and equal delivery/idempotency IDs matching `^delivery-[0-9a-f]{64}$`; return the actual reread hash plus the same ID and remove the current missing-ID file-hash fallback. Keep `createProductionComposition({})` non-injectable and fixed to `config.state_root`.
  - Covers `REQ-M2-005`, `TEST-M2-009`, and `TEST-M2-010`.
  - R106 C1: through unchanged `applyControllerCommand`, reauthenticate only the caller-retained original complete signed DISPATCH at post-Freeze PR/HANDOFF against exact current State/admission and the last complete Ledger authorization-cycle record. Return exact existing `ALREADY_APPLIED` over unchanged State with original idempotency/body SHA, keep authority ephemeral, refuse any existing local pause, and make no write/effect; later `run` alone resumes.
  - R109 C2: implement the single approved `createLedgerObjectReader` export and reuse that exact object proof inside the private production Ledger factory. The reader accepts only the closed constructor/read requests, uses the pinned Git environment and fixed type/physical-commit/tree/path/blob commands, rejects an untrusted wrong tree immediately after physical commit, preserves existing first-parent merge semantics, returns only the existing Gateway union, and never exposes credentials, transport, full factory, parser/spawn seam, new deadline, cache, State, or recovery machinery. Core independently retains receipt/byte/slice/ref/current-delivery checks; a Test-owned Ledger is not Adapter proof.
- [ ] `TASK-M2-034` — Close failures, concurrency, cancellation, and late/partial results in the four production files without new machinery.
  - A remotely proven failure may become durable BLOCKED only after failure/BLOCKED Ledger and BLOCKED State readback.
  - Ledger unavailability/ambiguity retains the prior phase, writes/readbacks only the exact local pause, and forbids progress without claiming durable BLOCKED.
  - Preserve one mutex, four boundaries, WIP one, existing reason/action vocabulary, and permitted terminal writes.
  - Preserve the exact one-hour value as one shared deadline beginning before launcher artifact reads, with no reset, clock/timer/timeout injection, escalation, retry, or fallback. The separate real `012-L06` must observe the actual product SIGTERM and close, then require existing ordinary `INTERRUPTED/AGENT_EXITED` even when its handler exits zero with `signal:null`; after `deadline_fired` no artifact reaches RESULT/PASS. External cleanup is never product evidence. Repair section 7.4 retains its more specific post-STARTED signal/deadline `INTERRUPTED/RESULT_UNREADABLE` rule.
  - Covers `REQ-M2-006`, `TEST-M2-011`, and `TEST-M2-012`.
  - R106 C3: after the real `REGRESSION_FAILURE / REVISION` event is durable and BLOCKED State CAS fails, use existing public/local-pause transport for `POINTER_STATE_CONFLICT / MANUAL_CONTROLLER_STOP`; bind the canonical public `run` request hash, original State identity, and real event tip/ID/hash. If local-pause persistence also fails, return existing unpersisted BLOCKED with no local-pause ID. Never use `STATE_WRITE_FAILED`, retry/clear/bypass on restart, or begin REVISION automatically.
- [ ] `TASK-M2-035` — Controller reviews K2 evidence.
  - Prove the normal public chain, every named failure frontier, one causal repair to a new Candidate and full revalidation, attempt-one FAIL stop, remotely selected evidence, and final `AWAITING_CONTROLLER` readback.
  - K2 is evidence over the same Change and K1 Candidate chain; it is not a separate Change or alternate route.

## 5. GREEN, Regression, Retirement, and Verification

- [ ] `TASK-M2-040` — Run focused GREEN in order: definition/Regression, dirty-tree/stage/Candidate K1, Candidate validation/Validator, every shared-launcher/Handoff producer suffix, the separate true-hour `012-L06`, then all runnable delivery K2 evidence. Record the A2-mapped privileged proof/K2 leaves only as `USER_WAIVED / NOT_VERIFIED`, never PASS.
- [ ] `TASK-M2-041` — Run the complete M2 integration Test, all affected Coordinator/Git/mode suites, byte-identical WVEB suite, and canonical `tools/harness/validation/run`.
  - Record exact command, exit status, stable Test counts/IDs, file hashes, runtime, Git version, expected provider skip, and failure output if any.
- [ ] `TASK-M2-042` — Reconcile every changed/added Test asset against the Test Asset Lifecycle Ledger.
  - Identify retain/replace/retire and current consumer for every helper/fixture/Test; prove no unexplained bulk retirement or hidden compatibility fixture.
  - Controller must record Test Asset Retirement PASS before implementation/evidence freeze.
- [ ] `TASK-M2-043` — Verify scope mechanically.
  - Production diff is limited to the four approved existing files; Test diff is limited to the frozen Test set; snapshot evaluator and WVEB Test body are byte-identical; no forbidden file or effect appears.
- [ ] `TASK-M2-044` — Freeze the implementation/evidence identity and dispatch a fresh read-only `juaner_validator`.
  - Validator checks the exact fixed Head/diff and returns independent evidence; it does not repair, approve, or modify files.
- [ ] `TASK-M2-045` — Controller decides M2 acceptance and only then moves to M3.
  - M2 acceptance does not install on a host, create a real GitHub PR/Handoff, integrate main, authorize provider/product activity, start Desktop, archive the Change, or close M3/M4.
  - Acceptance requires every unwaived runnable Host/Handoff/public-chain result and explicit A2 risk accounting. The privileged proof/dependent repair K2 positive remains `USER_WAIVED / NOT_VERIFIED`; M3 evidence cannot retroactively relabel it PASS.

## 6. File Ownership and Stop Conditions

### Future Worker production write set

- `tools/harness/change-coordinator/coordinator.mjs`
- `tools/harness/change-coordinator/production.mjs`
- `tools/harness/change-coordinator/adapters.mjs`
- `tools/harness/change-coordinator/host-loop.mjs`

There is no conditional production path. A need to change any other production, schema, snapshot, CLI, persistence, dependency, governance, or installation path is `BLOCKED / CONTRACT_CHANGE_REQUIRED` and returns to the Controller.

### Test write set

Only the exact minimum subset frozen by `TASK-M2-010` from the planned Test paths may change. Test ownership never permits production writes; Worker ownership never permits Test edits or weakened assertions.

### Immediate return conditions

- any deviation from the approved C2 object-reader boundary or any attempt to treat Core-double proof as production Adapter proof;
- any new semantic choice beyond C2-C7;
- inability to implement a closed field/hash/error/recovery rule in the four production files;
- a required fifth recovery boundary, second lock, extra event/state, compatibility projection, source rewrite, or Core mock;
- a change to accepted WVEB WORKTREE behavior or Test body;
- a request for a real provider/model/network/GitHub/host/product action.
