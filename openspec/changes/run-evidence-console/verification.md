# Verification Read Model: Xanthil Run & Evidence Console

## Current Verdict

`VALIDATION_FAIL_2 — SYSTEMATIC_TEST_CORRECTION_REQUIRED`

This file is the sole current lifecycle-status authority within this OpenSpec package. The Controller-owned project board mirrors it for observability only; Proposal, Design, Tasks, and traceability retain stable decision, architecture, work, and evidence mappings.

The first independent Validator returned material findings in persisted confirmed-contract admission, two-phase Adapter read order, neutral provenance shape, textual asset rendering, and error classification. After two Test/Worker repair cycles, a fresh independent Validator confirmed the prior version, outer-view, identity-order, and pre-read-size mechanisms but found three remaining gaps: unclosed Finding content semantics, the real Profile mapping pre-read size disagreement to read failure rather than checksum mismatch, and nested/non-success public view types that remain open. These are existing approved AC obligations, not new product scope or a contract decision. The repeated return is traced to example-based boundary tests rather than a missing architecture mechanism. Production and prior GREEN evidence are frozen while Test closes the full named boundary; no module, shared codec, Runtime abstraction, or new path is authorized.

## Accepted Authority and Frozen Scope

- REC-CONTRACT-001, REC-CONTRACT-002, and REC-CONTRACT-003 are accepted; no other Artifact, writer, Runtime, Profile, CLI, package, or shared-path change is authorized.
- The only production repair paths remain `apps/console/xanthil-console.ts`, `packages/application/run-evidence-query.ts`, `packages/product-core/run-evidence.ts`, `packages/ports/run-evidence-reader.ts`, `adapters/storage-local/run-evidence-reader.ts`, and `profiles/personal/console.ts`.
- The only test repair paths remain the eight REC-CONTRACT-002 paths plus REC-CONTRACT-003's named `TEST-XCLI-021` path; `tsconfig.json` remains conditional for its exact 14-path append only.
- Existing Core reuse remains Run Manifest/Evidence Index validation; reader Core owns exact persisted confirmed-contract admission. New viewer provenance is Artifact-`1.0`-only and creates no Runtime replacement/registry/fallback/second-Runtime contract.

## Reopened Gate Conditions

| Gate / condition | Owner | Release evidence |
|---|---|---|
| R2 repair re-review | Controller / Sol high | `PASS`; bounded `gpt-5.6-sol` / high review returned `SPEC_GATE_RECOMMEND_PASS` with no findings |
| Reopened Spec Gate | Controller | `PASS` on 2026-08-23; revised ACs, Design, Tasks, Test Plan, traceability, and simplification review frozen for Test |
| Corrected Test Design / RED | Test role | `REOPENED`; add all three Finding nonempty leaves, correct Profile size-mismatch checksum expectation, and close every named nested/status-discriminated public view type; exact TypeScript graph coverage remains solely TEST-XCLI-021 |
| Repaired Worker GREEN | Worker | prior `59/59` candidate invalidated by independent findings; production remains frozen until renewed TDD_READY |
| Regression / retirement | Controller | prior PASS preserved as history and invalidated; rerun after repaired GREEN |
| Independent validation | Validator | `FAIL`; three exact recheck conditions recorded above; fresh read-only validation required after repair |
| Acceptance / archive | Controller | all prior rows PASS or authorized waiver |

## Required Repaired Evidence

- Controlled temporary-workspace snapshot covers selected Run and outside paths; success fixture declares nonexistent/unreadable `sources[].path`; no mutation or source dereference.
- An unaccepted manifest path produces no second-file read/atime probe; manifest-indexed assets retain exact declared `byte_size` plus SHA checks, while core analysis-contract/Evidence descriptors remain SHA-only.
- Non-`1.0` `schema_version` in each named `run.json`, `analysis-contract.json`, and `evidence.json` independently maps to `RUN_CONTRACT_UNSUPPORTED`; fatal UTF-8 and duplicate object members at every depth, closed confirmed-contract nested field/enum/format/cross-reference defects, unknown Evidence fields, and invalid Finding status map to `RUN_READ_FAILED`.
- Only shape-valid duplicate/foreign/dangling/wrong-kind/unresolvable relationships map to `RUN_REFERENCE_INVALID`; declared core SHA and indexed asset size/SHA mismatches map to `RUN_CHECKSUM_MISMATCH`.
- E2E renders accepted SQL/Python/JSON/Markdown as labelled escaped inert `<pre>` display text, never numeric byte arrays or executable content.

## Superseded Candidate Evidence

- Controller focused Console suite: `59/59` PASS (`12` top-level tests and `47` parameterized subtests).
- Root `npm run typecheck`: PASS.
- REC-CONTRACT-003 successor TEST-XCLI-021: `1/1` PASS and remains the sole exact 35-file TypeScript graph oracle.
- `tools/harness/validation/run`: PASS; the approved real-Pi acceptance leaf remains skipped by the offline runner.
- All eight Console test/fixture/helper assets: `node --check` PASS.
- Test Asset Retirement Gate: PASS; the duplicate TEST-REC-010 graph block was removed with retained TEST-XCLI-021 successor coverage, and no orphan or retirement candidate remains.
- `git diff --check`: PASS.

The listed GREEN commands prove the superseded candidate only; they do not satisfy the second Validator recheck conditions or authorize acceptance. Prior focused `17/17`, `42/42`, and prior retirement records remain history only. Project-control is Controller-owned and mirrors this return for observability.
