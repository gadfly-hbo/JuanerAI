# Tasks: Model Pack Contract Enabler

## Task Plan

| Task | Owner and Gate | Output / write scope | Maps |
|---|---|---|---|
| `TASK-001` Complete R2 decision package | `juaner_spec`; complete | this Change's OpenSpec files only; preserves the public surface, prior permission-oracle disposition, and completed closed-graph correction while distinguishing the exact paired Node v26 source-loader chains from product filesystem effects | REQ-MPC-001..005 |
| `TASK-002` Repeat Spec Gate and lean review | Controller; PASS | nested `openSync` loader-chain root cause, paired-chain/no-mechanism disposition, full-package consistency, and mandatory complete-diff ponytail review | all |
| `TASK-003` Package Test Design and RED | `juaner_test`; complete and frozen | 625-line/`4db0316d…` suite unchanged | REQ-MPC-001, 004 |
| `TASK-004` Runtime/inactive Test Design and RED | `juaner_test`; Validator 006 return complete | 492-line/`f2685ff8…` suite adds three exact causal leaves; every other Test frozen | REQ-MPC-003..005 |
| `TASK-005` TDD_READY | Controller; PASS | Runtime 82/86 and E 317/321 fail only on three leaves plus one parent; Runtime-only release | all |
| `TASK-006` Implement contracts | `juaner_worker`; complete and frozen | package production remains frozen | REQ-MPC-001, 002, 005 |
| `TASK-007` Implement Runtime Port | `juaner_worker`; active after renewed TDD_READY | Runtime-only Unicode-control correction | REQ-MPC-003, 005 |
| `TASK-008` Integrate exact graphs | `juaner_worker`; complete and frozen | existing exact E entries in `tsconfig.json` and exact E commands in `tools/harness/validation/run`; no further edit | REQ-MPC-004, 005 |
| `TASK-009` GREEN and regression | historical PASS; reopened | Validator 006 found three missing material Runtime leaves | all |
| `TASK-010` Test Asset Retirement Gate | historical PASS; reopened | repeat after all three new leaves reach GREEN | REQ-MPC-004 |
| `TASK-011` Independent verification | Validator 006 FAIL | fresh Validator only after renewed RED/GREEN/retirement freeze | all |
| `TASK-012` Accept, integrate, archive | Controller/user according to risk | acceptance, strict E-first integration, current spec merge and archive; Controller-only project-control | all |

## Test-owned Exact Paths

- `tests/contract/model-pack-contract-enabler/model-pack-package.contract.test.ts`
- `tests/contract/model-pack-contract-enabler/analytical-model-runtime.contract.test.ts`
- `tests/integration/model-pack-contract-enabler/contracts-inactive.integration.test.ts`
- `tests/fixtures/model-pack-contract-enabler/model-pack-fixtures.ts`
- `tests/fixtures/model-pack-contract-enabler/model-pack-package-driver.ts`
- `tests/fixtures/model-pack-contract-enabler/analytical-model-runtime-driver.ts`

Previously conditionally released for the completed closed-graph correction and
now byte-frozen:

- `tests/integration/xanthil-local-analysis/local-analysis.integration.test.ts`
  — contains the eight exact approved E file entries in
  `approvedTsconfig.files`; preserve its 3,096 lines, SHA-256
  `b5c43e3bf65faab5e75c60a481c8c22aa966690c56b22a465622ab2a18dc433f`,
  `TEST-XCLI-021`, and every other assertion.

The Worker did not edit these paths. The Test Agent did not edit production,
root configuration, docs/contracts, OpenSpec, or project-control. After the
bounded Test Asset Retirement cleanup, every current Test asset is byte-frozen
at the post-GREEN hashes in `test-plan.md`; no further Test write is authorized.

## Current Worker-owned Remaining Path

- `packages/contracts/model-pack.ts`
- `packages/ports/analytical-model-runtime.ts`

`docs/contracts/model-pack.md`, `tsconfig.json`, and
`tools/harness/validation/run` are complete and byte-frozen for this Worker
return.

The Worker may edit only `packages/ports/analytical-model-runtime.ts`. It does
not edit package production, tests, Profiles, CLI, Product Core, Application,
SDK/Provider/Consumer, package/lock files, OpenSpec, project-control, or any
external repository.

## Stop Lines

- The prior comparator Gate and TDD_READY are historical. The permission-oracle
  contradiction reopened both and froze partial production. A new Spec Gate
  may release only a bounded Test correction: delete the seven unreachable
  preflight permission leaves, add the missing greater-than-56-day positive
  input leaf and factory invalid-value leaves, and preserve every other frozen
  mutation and helper.
- The later permission-oracle Test correction is also historical. The
  inactive-loader/closed-graph Gate and Test return are now historical:
  `TEST-XCLI-021` is corrected and frozen, while Node v26.0.0 exposed the nested
  `openSync` loader-chain conflict. A new Gate may release only one Test edit,
  the inactive oracle's exact paired-chain correction. Every other Test asset
  and assertion remains byte-frozen.
- The source-loader exclusion may match only the resolved contract and Port
  source targets and only an allowed `readFileSync` plus its synchronously
  nested `openSync`. Both observed target sets must equal exactly the two
  approved files and both exclusions end when the imports settle. A non-nested
  `openSync`, late same-path call, third target, any other filesystem
  read/stat/open, or any network/process/predictor/registration/Profile/CLI
  effect remains a failure. A product permission, production seam, source copy
  or string scan, custom loader/callback, registry, bypass, eager import,
  blanket observer disablement, platform abstraction, new dependency, glob, or
  graph relaxation is forbidden.
- Any need for an unlisted field/value/default/error, persistent state, schema store, dependency, Artifact or release-status lookup/service/list/cache/watcher/registry, filesystem/MLflow dereference, real data/model/network permission, production clock/scheduler hook, Provider/SDK placeholder, Product Core/Application/CLI/Consumer, or Profile returns to Controller before work continues.
- A contract mismatch found by P or C is a Contract Change Request; neither implementation edits shared contracts or drivers.
- A second Spec clarification, second correction for the same Test invariant, second Worker revision/replan, unexpected broad graph/test migration, or contradictory Head/evidence triggers complexity root-cause return.
- Test failure never authorizes weakening an AC, error mapping, reachable mutation, driver, assertion, permission, or inactive boundary. Deleting an unreachable duplicate preflight permission oracle must preserve each manifest permission-widening lower-boundary assertion.
- E cannot be accepted or merged out of strict E -> H -> P -> C -> A order.
